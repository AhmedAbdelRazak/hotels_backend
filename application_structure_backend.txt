JANNATBOOKING BACKEND APPLICATION STRUCTURE
Audited: 2026-06-03

Purpose
This file is a compact backend reference for future development. It documents the active Express/Mongo API structure, the important route files, the user-role model, hotel scoping rules, and the workflows that must be protected when changing the system.

Runtime Entry Point
- server.js
  - Creates the Express app, HTTP server, Socket.IO server, MongoDB connection, middleware, and route mounting.
  - Loads every file in ./routes dynamically under the /api prefix.
  - Serves static uploads from /uploads.
  - Starts the housekeeping maintenance scheduler after MongoDB connects.
  - Socket rooms currently used:
    - support case rooms by support-case id.
    - housekeeping:{hotelId} for housekeeping updates.
    - hotel-notifications:{hotelId} for hotel notification counters.
    - owner-notifications:{ownerId} for owner-level notification counters.
  - Watches SupportCase changes and broadcasts the latest conversation activity.

Top-Level Backend Structure
- aiagent/
  - AI agent orchestration, learning, policy, memory, ingestion, and reservation support utilities.
- controllers/
  - Business logic for every domain: auth, users, hotels, rooms, reservations, housekeeping, overall dashboards, reports, payments, support, OTA tools, and wallet operations.
- models/
  - Mongoose schemas for users, hotels, rooms, reservations, housekeeping tasks, support cases, expenses, agent wallets, activity tracking, and integration data.
- routes/
  - Express route definitions. Most routes delegate to controllers and are loaded by server.js under /api.
- services/
  - Shared backend services for notifications, activity tracking, reservation pricing/status, OTA email mapping, and housekeeping maintenance.
- uploads/
  - Uploaded hotel images, user documents, reservation documents, and generated/imported assets.

Core User Roles And Scope Rules
- role 1000
  - Platform admin / super admin level. In business terminology, Super Admins are the platform owners/operators, and platform admins belong to the JannatBooking platform side.
  - Also treat REACT_APP_SUPER_ADMIN_ID and SUPER_ADMIN_ID style configured users as super admins where used.
  - Backend helpers must also recognize roleDescription/roleDescriptions values such as superadmin and super admin as platform-super-admin grants.
  - Can access admin routes, owner-style hotel operations, account management, hotel-staff updates, and cross-hotel reporting without being restricted by hotelIdWork, hotelIdsWork, hotelIdsOwner, hotelsToSupport, or belongsToId.
- role 2000
  - Root hotel owner when the account has no belongsToId.
  - Also appears for scoped hotel managers when roleDescription is hotelmanager and hotelIdWork / hotelIdsWork / belongsToId are set.
- role 10000
  - Hotel System Admin for an owner account. This is still a hotel-side employee/admin for one hotel or a group of hotels.
  - Broad owner-like access, but always scoped to assigned hotels through hotelIdsOwner, hotelsToSupport, hotelIdWork, or belongsToId.
  - Keep the internal roleDescription value as systemadmin for compatibility, but user-facing UI and documentation should call it Hotel System Admin, not generic System Admin.
- role 3000
  - Reception. Reservation-facing access for assigned hotel scope.
- role 4000
  - Housekeeping manager. Housekeeping reports, task assignment, and operational task management for assigned hotels.
- role 5000
  - Housekeeper. Assigned housekeeping task access.
- role 6000
  - Finance. Financial dashboards, reports, wallet/commission actions, and selected reservation reporting within hotel scope.
- role 7000
  - Order taker / external agent. Usually limited to newReservation and ownReservations access. Booking source should default to the agent/company identity.
  - Public agent signup creates this role as inactive with agentApproval.status = pending and applicationReview.status = pending until a hotel owner/manager approves it.
- role 8000
  - Reservation employee. Reservation operations, pending confirmations, and selected settings/report access.
- role 0
  - Legacy/client/public-style role still appears in older code paths and should be treated carefully before removal.

Role Data Model Notes
- models/user.js is the source of account and staff-role data.
- Important fields:
  - role: primary numeric role.
  - roleDescription: primary text role.
  - roles and roleDescriptions: additional role grants.
  - belongsToId: parent owner/account id for staff or scoped users.
  - hotelIdWork and hotelIdsWork: assigned hotel scope used by hotel staff flows.
  - hotelIdsOwner and hotelsToSupport: owner/hotel-system-admin support scope.
  - accessTo: feature-level access such as newReservation and ownReservations.
  - activeUser: account activation gate.
  - companyName, agentCommercialModel, agentApproval, wallet opening balances: agent/ordertaker finance context.
  - applicationReview: public signup review state for agent and job applicants before hotel approval.

Critical Backend Authorization Helpers
- controllers/auth.js
  - createHotelStaffUser maps role descriptions to numeric roles and validates that the creator can create accounts for the requested hotels.
  - Finance and reservation employee roles can create external agent/order-taker accounts in specific paths.
  - isPlatformSuperAdmin is the global admin source for auth middleware and staff creation. isAuth, isAdmin, isHotelOwner, agent approval, and staff-creation checks should use this broad helper instead of only checking a single configured id or role number.
- controllers/overall_dashboard.js
  - requireOverallSection, canAccessOverallSection, and getAccessibleOverallHotels are the main overall-dashboard guards.
  - Owner-like access is not the same as global access. Hotel System Admins and scoped managers must still be limited to assigned hotels.
  - Platform super admins must load overall hotels globally by default. ownerId may narrow the view, but stale hotelIdWork/hotelIdsWork/hotelIdsOwner/hotelsToSupport values on the super-admin account must never restrict global access.
  - Overall reservation list sorting supports the frontend sort controls for booking source, hotel name, arrival date, and departure date through sortFromQuery.
- controllers/admin_global_hotel_settings.js
  - Owns platform-admin global hotel settings workflows for all hotels across the platform.
  - This controller intentionally does not expose or reuse the hotel-management /overall-dashboard/settings routes.
  - Access is route-guarded by requireAdminAccess("AdminDashboard", "HotelReports"), so configured SUPER admins and role 1000 platform admins with those access keys may use it.
  - Reuses shared services/overallRoomTextOrchestrator.js and services/overallCalendarPricingOrchestrator.js for bilingual room text and calendar row construction while keeping a separate admin-global controller and route namespace.
- controllers/reservations.js
  - canCreateReservationForActor and canViewReservationHotel protect reservation creation and visibility.
  - Order takers are restricted to their allowed reservation flows and own reservation records.
  - Reservation status progression and backward status changes must remain protected.
- controllers/user.js
  - canManageHotelStaff protects staff/account management by hotel scope.
  - isPlatformSuperAdmin must be treated as an unrestricted staff/account-management actor. Super admins can update any hotel staff or owner-like account through PUT /hotel-staff/:staffId/:hotelId/:userId.
  - updateHotelStaffUser must recognize root owner accounts by _id === ownerId and hotelIdsOwner, not only belongsToId. Root owner accounts normally have no belongsToId and should not be rejected as "not belonging to this hotel".
  - Agent and employee activation from the overall activate-accounts screen should still go through updateHotelStaffUser, not the Hotel System Admin endpoint, so activeUser, agentApproval/applicationReview, approvedHotelIds, notifications, and ActivityTracker updates remain consistent.
- controllers/agent_wallet.js
  - Financial access guards protect agent wallet, claims, transactions, and commission review.
  - Agent wallets are global per agent, not per hotel. Wallet ledger rows should use agentId as the financial scope; hotelId is nullable and legacyHotelId is only audit/display context from older hotel-scoped records.
  - Wallet summaries and transactions must calculate deposits/claims/manual adjustments across the agent's global wallet, while reservation usage remains filtered to the acting user's authorized hotel scope.
  - Agent wallet claims no longer accept or require a hotel selection. Finance/manager/admin users can review global agent claims if the agent belongs to one of their accessible hotels; agents can only submit/read their own wallet.
  - Wallet claim statuses drive finance reporting: posted/approved movements are accepted and can affect wallet totals; pending/rejected/void movements must remain visible for audit but are reconciliationEligible = false and must never be included in reservation reconciliation totals. Both status and reviewStatus must be checked so mixed legacy records cannot slip into totals.

Primary Route Files And API Areas
- routes/auth.js
  - Signup, property listing, signin/signout, password reset, Google login, and hotel staff account creation.
  - Key routes include POST /signup, GET /signup-invitation, GET /signup-invitation/:code, POST /signin, POST /property-listing, POST /hotel-staff/create/:userId.
  - POST /property-listing supports owner signup, public agent applications, and public job applications.
- routes/user.js
  - User profile reads/updates, admin account lists, hotel staff lists, staff updates, housekeeper lookup, and preview dashboard data.
  - GET /hotel-agents/:hotelId/:userId returns a scoped, minimal, searchable approved-agent option list for room inventory/pricing assignment UIs.
- routes/hotel_details.js
  - Hotel profile creation/update, owner/admin hotel lists, executive summaries, open/incomplete reservation summaries, hotel stats, reassignment, and owner PayPal methods.
  - Public hotel-detail reads use optional auth and strip room-level agentInventory/agentPricingRate unless the viewer is the agent for those rows or an authorized settings user.
  - PATCH /hotel-details/:hotelId/rooms/:roomId/agent-overrides/:userId persists only a room's private agentInventory or agentPricingRate override data. It must not save the whole hotel/room form, so unrelated unsaved settings changes are not committed by the agent override modal.
  - The same agent-overrides PATCH endpoint supports agent-specific blocked calendar dates by saving agentPricingRate rows with price=0, rootPrice=0, color=black, status=blocked, blocked=true. Assigned reservation employees, system admins, hotel managers, owners, and platform admins can save these rows when scoped to the hotel.
- routes/rooms.js
  - Room CRUD, distinct room values, room inventory availability, reservation-room availability, room deletion checks, and inventory reports.
- routes/reservations.js
  - Reservation creation, update, search/list, import preview/commit, pending confirmations, occupancy/current/range reports, payment links, guest emails, OTA dumps, source/status reports, and owner aggregation reports.
- routes/overall_dashboard.js
  - Multi-hotel overall dashboard endpoints: summary, reservations, exports, pending reservations, financial actions/report export, housekeeping, accounts, hotel-system-admin actions, and settings.
- routes/admin_global_hotel_settings.js
  - Platform-admin global hotel settings endpoints under /api/admin/global-hotel-settings/*.
  - These routes are separate from /api/overall-dashboard/* and are meant for all-hotels platform administration, not owner-scoped hotel-management screens.
- routes/housekeeping.js
  - Housekeeping task creation/list/count/update, assigned employee task views, supplies, and supply requests.
- routes/agent_wallet.js
  - Agent wallet summaries, todos, claims, transactions, reviews, voids, and adjustments.
  - Primary wallet routes are global by agent/actor: /agent-wallet/summary/:userId, /agent-wallet/todos/:userId, /agent-wallet/claims/:userId, /agent-wallet/claims/:userId/:transactionId/review, and /agent-wallet/transactions/:userId. Older /:hotelId/:userId forms remain only for compatibility and must not reintroduce hotel-scoped wallet logic.
- routes/expenses.js
  - Expense CRUD, hotel financial-report data, and expense support endpoints.
- routes/adminreports.js
  - Admin and hotel reports, reservation/check-in/check-out reports, top hotels, export, paid breakdown, and occupancy.
- routes/hotel_inventory.js
  - Calendar/day/availability inventory endpoints.
  - Availability supports an authorized agentId query to return agent-scoped assigned stock/pricing while falling back to hotel-wide inventory/calendar when no override exists.
  - Agent-scoped availability must count pending-confirmation reservations against that agent's assigned stock. This is intentional because order-taker/agent reservations start as Pending Confirmation, and the same agent must not be able to place multiple pending reservations against stock=1.
- routes/supportcase.js
  - Support cases by status/hotel, case details, updates, unseen counters, seen status, and message deletion.
- routes/janat.js
  - Public website and client booking APIs: active hotels, single hotel by slug, public room query, client reservations, payment verification, invoices, user reservations, currency, public payment flows, WhatsApp confirmations, and admin public-site reservation tools.
- Payment and payout route files
  - paypal_owner.js, paypal_reservation.js, braintree.js, braintree_reservation.js, stripe.js, square.js, bofaprocessing.js, and admin_payouts.js.
  - 2026-06-04 checkout account safety: controllers/paypal_reservation.js findOrCreateUserByEmail creates a client account during public checkout when no existing email/user is found. It must accept customerDetails.password, customerDetails.usePassword, or a phone-derived fallback so paid checkout does not fail after PayPal approval because the guest was not already signed in.
  - 2026-06-04 PayPal capture safety: controllers/paypal_reservation.js must treat the nested capture status as the source of truth. A PayPal order may be APPROVED or COMPLETED while purchase_units[].payments.captures[].status is DECLINED; that is not a paid reservation and must return 402 with a clear message that Jannat Booking did not receive a successful captured payment.
- Integration route files
  - otaInbound.js, integrator.js, and aiagent_learning.js.

Core Models
- models/user.js
  - Account, role, staff scope, agent/company, and access data.
  - notificationAcknowledgements stores per-user read receipts for generated dashboard notifications. Use stable ack keys so resolved agent-facing wallet/reservation notifications disappear after click without hiding pending operational queues.
- models/signup_invitation.js
  - Stores short public signup invitation records for /signup?invite=<code>. Codes are stored only as SHA-256 hashes; payloads include signed invitation scope such as account type, hotels, hotel names, role, agent commercial model, optional prefill data, creator, and expiry.
- models/hotel_details.js
  - Hotel profile, owner link, setup completion fields, rooms/photos/location/payment settings, activation state, and wallet/payment fields.
  - roomCountDetails may include private per-agent agentInventory and agentPricingRate rows; do not expose them to public/B2C hotel responses.
  - 2026-05-27 regression: virtual reservation employee codex.virtual.res.employee.qa@jannatbooking.test assigned stock for virtual agent codex.virtual.agent.qa@jannatbooking.test on hotel 68da202900a070e8123c27c4 / room 68da23fe00a070e8123c2aa4. Stock=1 allowed exactly one agent reservation over 2032-04-19..2032-04-22 and blocked an overlapping 2032-04-20 request. Stock=0 then blocked 2032-04-29..2032-05-01. All assertions passed.
  - 2026-05-28 regression: temporary users codex.block.* saved an agent-specific blocked range on hotel 68da202900a070e8123c27c4 / room 68da23fe00a070e8123c2aa4. A reservation employee and a system admin could save the blocked range; agent availability returned blockedDates; an overlapping agent reservation returned agentCalendarBlocked; a non-overlapping reservation was allowed. The temp users, temp reservation, and temp agent override rows were removed and verified at zero remaining documents/rows.
- models/rooms.js
  - Physical room records, room type/display, room cleanliness state, housekeeping dirty metadata, bed setup, and hotel ownership.
- models/reservations.js
  - Reservation status, guest details, confirmation number, room ids, pricing, payment data, source/order-taker fields, pending confirmation state, commission snapshots, wallet snapshots, availabilitySnapshot, financial cycle, and audit fields.
  - availabilitySnapshot captures selected-room availability at reservation creation time. It stores the reserved stay dates, requested room count, lowest available rooms for the selected room lines during the stay before the reservation, remaining availability after the reservation, per-room/per-day capacity and reserved counts, and whether an authorized overbook override happened.
- models/housekeeping.js
  - Housekeeping tasks, room/task status, assignedTo/assignedBy, status history, start/end timestamps, duration, and hotel scope.
- models/agent_wallet.js
  - Agent wallet balances, claims, transactions, review/void flows, and commission tracking.
  - Wallet rows are keyed by agentId for balance math. hotelId is optional/null for current rows; legacyHotelId preserves the previous hotel context for historical rows and notification/report display only.
- models/expenses.js
  - Hotel/account expenses and financial-report support data.
- models/supportcase.js
  - Support cases, conversations, seen/unseen tracking, and case status.
- Supporting models
  - activity_tracker, housekeeping_supply, housekeeping_supply_request, Uncompleted, aiagent learning models, and integration/inbound data.

Important Services
- services/housekeepingMaintenance.js
  - Scheduled job that marks rooms dirty after the configured clean-room window.
- services/supportCaseMaintenance.js
  - Scheduled job that closes inactive open B2C client SupportCase records after 1 hour.
  - It must only set the case to closed and preserve the SupportCase document and conversation history.
- services/notificationEvents.js
  - Emits hotel and owner notification counter updates through Socket.IO rooms.
- services/reservationStatus.js
  - Shared pending-confirmation and reservation-status helpers.
- services/pendingConfirmationPolicy.js
  - Shared creation policy for internal hotel review. Use markReservationPendingConfirmation when a new booking should enter the hotel confirmation queue.
  - The policy can set reservation_status/state to Pending Confirmation for operational staff flows, or keep client-facing status confirmed while pendingConfirmation.status="pending" drives the hotel queue.
- services/reservationPricing.js
  - Reservation pricing calculations and shared pricing behavior.
- services/agentRoomOverrides.js
  - Shared helpers for per-agent room inventory/pricing overrides, agent identity resolution, and authorization checks for agent-scoped availability.
- services/activityTracker.js
  - Writes operational activity/audit history.
  - Tracks reservation status/scope changes, reservation exports, financial report exports, account creation, and account updates.
  - Account tracking stores safe before/after field changes for role, hotel scope, active state, access, agent wallet setup, approval status, and company-document metadata only. It must never store plaintext passwords, password hashes, or full attachment data.
- services/auditPrivacy.js
  - Central privacy guard for reservation/admin tracker visibility.
  - Reservation adminChangeLog and reservationAuditLog entries made by superadmin/admin/systemadmin actors are removed from backend responses unless the requesting viewer is a true super admin.
  - Privileged actor snapshots such as adminLastUpdatedBy, pendingConfirmation.lastUpdatedBy, agentDecisionSnapshot.decidedBy, financial_cycle.*By, and commission approval reviewers are also nulled for non-super-admin viewers so ordinary agents/staff never see platform admin names in reservation details.
  - Optional Authorization headers are respected on public-safe single-reservation fetches so super admins can see privileged tracker rows while public/client views and ordinary hotel users cannot.
  - Public-safe reservation fetches must tolerate an unauthenticated/null viewer and treat it as a client viewer. Passing null into audit privacy helpers must never crash a payment link.
  - ActivityTracker records created by privileged actors are marked visibility=super_admin_only for future reporting surfaces.
- services/otaEmailOrchestrator.js and services/otaReservationMapper.js
  - OTA email/reservation parsing and mapping support.

Critical Workflows To Protect
- Reservation creation
  - Always verify the acting user can create a reservation for the target hotel.
  - For overall-dashboard reservation creation, require the hotel dropdown to be selected from hotels assigned to the user.
  - For agents/order takers, source should come from the agent/company identity unless a role explicitly requires a manual source.
  - If a room has assigned stock for the booking agent, reservation inventory validation counts against that agent stock first and only counts reservations created by that same agent.
  - If a room has agent-specific calendar pricing for the booking agent, reservation pricing uses that row first and falls back to the hotel-wide room calendar otherwise.
  - New reservations must persist availabilitySnapshot from the same validateReservationInventoryForCreate result used for inventory blocking/overbook warnings. Do not let the frontend submit this count as trusted data; backend creation paths should stamp it after validation for hotel-management, public/PayPal, employee-direct, verification, admin Order Taker, OTA/integrator imports, Excel imports, and AI-assisted creation flows.
  - 2026-06-04 mandatory hotel confirmation: newly created reservations from admin Jannat Tools Order Taker, public/client booking, PayPal checkout, payment verification, and chatbot-assisted public booking paths must set pendingConfirmation.status="pending" so the hotel team confirms them before the internal cycle closes.
  - Manual admin/Jannat Tools Order Taker creation must also set reservation_status/state to Pending Confirmation because the hotel team should see it as an operational pending item immediately.
  - 2026-06-04 hotel-management manual creation: POST /api/reservations/create/:userId/:hotelId must force the reservation cycle for every new manual reservation, including admin and SUPER ADMIN actors. The frontend may still send Confirmed/InHouse, but the backend must save reservation_status/state as Pending Confirmation and pendingConfirmation.status as pending, then move through hotel confirmation -> finance confirmation -> confirmed via workflow actions.
  - Admin/SUPER ADMIN manual creation keeps admin flexibility for pricing/calendar/inventory overrides; only order-taker/agent actors keep the stricter agent inventory and blocked-calendar rules. Do not use the pending-cycle flag to apply agent commission defaults to platform admins.
  - Paid/public/client-created reservations should keep clientVisibleStatus="confirmed" and should not expose pending-confirmation wording in public invoice, payment-link, WhatsApp, or single-reservation responses. The internal pendingConfirmation object is for hotel/admin workflow only.
  - Public/admin-created pending confirmations must keep pendingConfirmation.inventoryBlocks=true so the booking remains counted against public/hotel availability while the hotel team reviews it. Do not make paid public bookings reappear as available inventory just because they are internally pending confirmation.
  - controllers/reservations.js pending-confirmation list/notification filters must include pendingConfirmation.status="pending" in addition to text statuses, otherwise paid/public bookings can be marked internally but hidden from the hotel queue.
  - 2026-06-04 notification/card alignment: the admin pending notification badge must include reservations whose visible status/state is explicitly "Pending Confirmation" even if their booking date predates the 2026-05-08 workflow cutoff. The cutoff still protects older finance/commission cleanup items so historical no-commission rows do not flood the badge.
  - 2026-06-04 admin reservation scorecards/filter: /api/admin/all-reservations pendingConfirmation counts and filters use operational pending status, meaning reservation_status/state is "Pending Confirmation" or pendingConfirmation.status is "pending". This keeps the card, clicked table rows, and hotel notification badge aligned for public-paid/internal-pending bookings.
  - 2026-06-04 overall pending queue: /hotel-management/main-dashboard?overall=pending-reservations uses the same explicit pending-confirmation bypass for pre-2026-05-08 rows, so notification clicks do not land on a queue that hides the reservation. Finance/commission backlog items still require the workflow cutoff.
  - Historical/import creation can allow overbook for compatibility, but should still capture the snapshot with overrideAllowed/overbooked metadata so reservation staff can audit what inventory looked like when the row was written.
- Public signup applications
  - Direct public signup is for hotel manager/owner registration only.
  - Agent and job applicants require a valid backend invitation generated from the overall create-account flow. New invitations use a short /signup?invite=<code> URL backed by models/signup_invitation.js; legacy invitationToken links are still accepted for compatibility.
  - GET /signup-invitation/:code resolves a short code only after checking the stored hash, expiry, revoked state, and invited hotel records. It returns public-safe hotel names/locations and locked invitation fields for the frontend.
  - POST /property-listing must remain the source of truth for invitation validation, hotel validation, and pending approval creation. It accepts invitationCode first and falls back to invitationToken.
  - Signed invitations may include assigned hotels that are not public/published on the Janat frontend; public non-invited agent applications must still use active published hotels.
  - If a pending applicant requested hotels across owners, approval must scope the account to the approving owner's hotel(s), not blindly activate every requested hotel.
  - Application attachments are stored in companyDocuments and may include company files, CVs, or wallet/payment proof.
  - Emit hotel/owner notification refresh events when a public application is submitted or approved.
  - Keep applicationReview and agentApproval in user read/auth payloads because the pending applicant dashboard refreshes its approval status from /user/:userId.
  - Agent-facing notifications
  - GET /reservations/notifications/pending-confirmation/:userId generates reservation, finance, account, and wallet notification feeds. Resolved agent wallet claims and accepted/rejected reservation decisions include ackKey/ackable fields. Frontend finance wallet notifications should carry the walletTransactionId into overall=wallet-management&walletTab=update so the selected claim/movement opens in update mode.
  - PUT /reservations/notifications/pending-confirmation/:userId/acknowledge stores the ack key on the user. Pending work-queue items should not be made ackable; resolved account approvals/rejections, wallet claim approved/rejected, and reservation/finance accepted notifications should disappear after one click.
- Reservation status changes
  - Prevent non-super-admin users from moving reservation/task status backward.
  - Closed/completed records should not be reopened except by a super admin path that also clears start/end timestamps where required by the business rule.
- Tracker privacy
  - Any reservation tracker/audit update performed by a super admin, platform admin, or Hotel System Admin must only be visible to true super admins.
  - Apply services/auditPrivacy.js before returning reservation details, pending-reservation queues, finance/payment commission rows, reservation searches, and report rows that include adminChangeLog or reservationAuditLog.
  - Sanitize both audit arrays and standalone "last updated by" snapshots before responding; hiding the table rows alone is not enough because ReservationDetail also renders workflow summary metadata.
  - Do not rely on frontend hiding alone for privileged audit rows. Backend responses should already be sanitized for non-super-admin viewers and for unauthenticated public/client payment fetches.
- Housekeeping task assignment
  - Managers and authorized hotel users can assign/update housekeeping tasks within assigned hotel scope.
  - Housekeepers must only receive and act on tasks assigned to them or scoped to their hotel.
  - Notifications must be emitted through hotel/owner/housekeeping socket rooms when assignments or task states change.
- Overall dashboard
  - Use backend hotel-scope helpers for every multi-hotel query. Never trust ownerId or hotelId query values without checking the actor's allowed hotels.
  - Platform super admins are the exception to hotel-scope limits and must remain global across ownerId/hotelId contexts.
  - Overall pending-reservations is available to approved order-taker/agent accounts as a read-only operational queue. Agents must only receive reservations tied to their own createdByUserId/orderTakeId/createdBy/orderTaker identifiers, while hotel staff keep their normal pending/finance workflow scope.
- Account management
  - Staff creation and updates must validate that the creator controls every hotel selected for the new or updated account.
  - Super admins can create, activate, update, approve, and scope accounts across any hotel. Do not add hotel assignment restrictions to role 1000 / configured super-admin / superadmin role-description users.
  - Root owner accounts should preserve hotelIdsOwner and avoid forcing belongsToId during account updates. Updating a root owner through staff-management routes must not convert it into a scoped staff account.
  - PUT /overall-dashboard/system-admin/:accountId/:userId and POST /overall-dashboard/system-admin/:userId are legacy route names for Hotel System Admin account management. They must validate hotel assignments differently by actor: super admins validate selected hotel IDs against real HotelDetails records globally; owner/hotel-system-admin/hotel-manager actors validate selected hotel IDs as a subset of their accessible overall hotels.
  - The system-admin update route may convert an existing managed account into a Hotel System Admin. It must force role/roles/roleDescription/roleDescriptions to systemadmin only, clear belongsToId, set hotelIdWork/hotelIdsWork/hotelIdsOwner/hotelsToSupport from the selected hotels, and preserve sanitized accessTo.
  - Staff creation with roleDescription systemadmin must also keep Hotel System Admin mutually exclusive, set hotelIdsWork/hotelIdsOwner/hotelsToSupport from selected hotels, and avoid assigning belongsToId like a regular hotel employee.
  - Converting a Hotel System Admin back to hotel manager/reception/finance/etc. is handled through the hotel-staff update route. That route must allow accounts scoped by hotelIdsOwner/hotelsToSupport, then clear hotelIdsOwner for non-system-admin non-root-owner accounts and restore belongsToId to the hotel owner.
  - Account create/update routes must call services/activityTracker.js. Staff creation uses hotel_staff_create, staff updates use hotel_staff_update, and Hotel System Admin create/update uses overall_system_admin_create / overall_system_admin_update.
  - Overall create-account is a two-mode frontend flow, but backend validation remains authoritative. Internal Staff mode must receive explicit hotel scope and a non-agent staff role; External Agents mode must receive explicit hotel scope and ordertaker/agent role data. Backend account creation and signup invitation creation should reject missing or mismatched hotel/role scope even if the frontend already validates it.
  - Overall account-management list filters must support the UI split between Hotel Employees and External Agents. The employee view excludes order-taker/agent accounts, while the agent view returns only order-taker/external-agent accounts inside the actor's authorized hotel/account scope.
  - Overall update-account mirrors the frontend Hotel Employees / External Agents accountView split and may preselect a matching accountId row for convenience. Backend update routes must continue validating account id, creator scope, selected hotels, and role changes because query params or prefilled UI state are never trusted.
  - Signup invitation creation in controllers/overall_dashboard.js must verify the actor controls every selected hotel, store the short-code invitation payload, and return only the generated code/token metadata needed by the frontend. Agent invitations should be locked to ordertaker semantics; employee invitations should require the requested employee role.
- Financial actions
  - Wallet, commission, payout, expense, and financial report actions must remain restricted to owner/admin/finance/reservation roles with valid hotel/account scope.
  - Approved order-taker/agent accounts may read overall financial surfaces only for their assigned hotels and their own agent wallet/reservation records. If financial-actions is opened by an agent, both reservation actions and pending wallet-claim rows must be filtered to that actor's own agent id.
  - Overall financial-actions returns both reservation finance actions and a separately paginated pending wallet-approval queue. Pending wallet claims are sourced globally by visible agentId from AgentWallet records with source=agent_claim, status=pending, and reviewStatus=pending; do not filter claims by AgentWallet.hotelId.
  - Overall financial-report wallet movements must expose financialStatus and reconciliationEligible so rejected claims can remain visible while staying excluded from wallet totals and reconciliation. Rejected rows are audit history only, not reservation reconciliation data.
  - Agent wallet summaries must return both commissionPaid and commissionDue totals. commissionPaid is the already-paid-out commission amount, while commissionDue is the unpaid/outstanding amount. Frontend reports and exports depend on this split for agent and finance reconciliation.
  - Agent opening wallet credit is a global account value. Per-hotel opening balance arrays should be sanitized to empty for new/update flows and should only be read as a legacy fallback when needed.
- Agent overall regression scenario
  - Create/approve an order-taker agent assigned to multiple hotels, then create reservations for that agent and for another agent in the same hotel set.
  - As the approved agent, GET /overall-dashboard/pending-reservations/:userId with ownerId and optional assigned hotelId. The response must include only the authenticated agent's pending/rejected/finance-review/commission-review reservations and must exclude every other agent's records.
  - As the approved agent, GET /overall-dashboard/financial-actions/:userId and GET /agent-wallet/summary/:userId, including attempts to pass another agentId. The backend must still return only the authenticated agent's global wallet, commission, reservation, and claim data.
  - Repeat with an unassigned hotelId. The API must not expose data for that hotel.
  - Repeat as reservation employee, finance, manager, Hotel System Admin, and super admin to confirm staff/admin queue visibility still follows their broader role scope and was not narrowed by the agent-only filters.
- Public website routes
  - Keep public Janat routes separate from authenticated hotel-management behavior. Public booking/payment endpoints still need reservation/payment validation.
  - GET /api/reservations/single-reservation/:reservationId is used by /client-payment/:reservationId/:confirmation and must return a client-safe reservation payload without requiring req.profile.
  - Public/client reservation responses must apply the client-visible status from pendingConfirmation.clientVisibleStatus and remove pendingConfirmation/agentDecisionSnapshot so guests do not see internal hotel-review state while a paid booking is operationally pending.
  - Optional encrypted customer payment fields may be blank, plaintext legacy placeholders, or encrypted values; single-reservation lookup must not fail the public payment page when optional card fields cannot be decrypted.
  - GET /api/support-cases-customer/:caseId/unseen-count returns { count } for the public chat badge and should return count 0 for missing/invalid/closed cases so public pages do not show noisy 404 polling errors.

2026-05-25 Implementation Log
- Role naming and access language
  - The hotel-side systemadmin role is now documented and displayed as Hotel System Admin.
  - Keep the stored/internal roleDescription value as systemadmin and the numeric role 10000 for compatibility.
  - Hotel System Admin is scoped hotel-side administration, not platform-wide administration.
  - Platform admins and true super admins remain separate: super admins are global platform owners, while Hotel System Admin users are limited to assigned hotels.
- Overall executive reports for hotel owners and authorized hotel staff
  - Executive summary data must use owner/hotel scoped backend routes instead of reusing global admin report routes directly.
  - Super admins can view across the platform; owners, Hotel System Admins, hotel managers, finance users, reservation employees, order-taker/agent users, and other permitted roles must be constrained to assigned hotels.
  - Use getAccessibleOverallHotels, requireOverallSection, and related overall-dashboard helpers before applying hotelId, invHotel, ownerId, or query filters.
  - Never trust ownerId or hotelId from the query string until it has been checked against the acting user's accessible hotel set.
  - The overall summary page has four frontend tabs: combined Hotels Summary, reservation analytics, inventory/occupancy, and paid reservations overview. The combined Hotels Summary embeds the former My Summary reservation-department dashboard above the hotels summary table, and it reuses the reservation analytics and overall reservation-list backends with the acting user's existing hotel/agent scope.
  - Admin global reports remain separate from hotel-management executive reports for security and data-boundary clarity.
- Reservation executive report backend behavior
  - The reservation analytics endpoint supports hotel, status, source, payment, search, dateBy, dateFrom, dateTo, includeCancelled, and excludeCancelled query semantics.
  - Order-taker/agent actors may access the executive reservation analytics route for the embedded Hotels Summary reservation-department report, but isOrderTakerOnly must continue adding the actor-created/order-taker reservation constraint before aggregation.
  - dateFrom/dateTo may be plain yyyy-mm-dd values or exact ISO timestamps. ISO timestamp support is required for chart drill-downs because report charts group by Asia/Riyadh day while Mongo date ranges are stored in UTC.
  - The overall reservation list endpoint supports bucketDateBy/bucketDateFrom/bucketDateTo in addition to the main dateBy/dateFrom/dateTo filters. The embedded Hotels Summary reservation-department report uses these bucket filters when a user clicks an arrival/departure bar so the original report range is preserved while the modal narrows to the clicked Asia/Riyadh day.
  - Executive reservation aggregates include roomNights and distinct source counts so the embedded Hotels Summary report can display nights and source scorecards without client-side shortcuts.
  - The embedded Hotels Summary "new reservations today" group is always createdAt-today in the Asia/Riyadh day window. It ignores the main report date range/dateBy/status/search filters, but still inherits actor authorization and selected hotel scope.
  - The embedded Hotels Summary "today's operation" group is also fixed to the Asia/Riyadh today window. Arrivals use checkin_date today, departures use checkout_date today, checked-in/no-show use the appropriate operational status plus today's operational date, and only hotel/actor scope should change those numbers.
  - The embedded future-operation chart is a fixed 29-day operational window: previous 14 Riyadh days, today, and next 14 Riyadh days. It switches between checkin_date and checkout_date by the frontend toggle, zero-fills dates with no activity, and keeps drill-down rows scoped to the clicked day plus actor/hotel authorization.
  - Overall reservation list responses used by reservation-main must include scorecards that match the same scoped filter set as the visible table. Clickable scorecard filterType values should never bypass getAccessibleOverallHotels, agent ownership checks, or the regular reservation match builder.
  - dateBy must map only to approved date fields such as createdAt, check-in/arrival, or checkout/departure.
  - Cancelled, no-show, and pending reservations must be counted only when the filter or include/exclude setting says so. Do not silently exclude them when a user explicitly asks for those statuses.
  - The overall reservation list endpoint also accepts excludeCancelled=true for scorecard/chart drill-down modals. This keeps modal rows aligned with reservation analytics totals while still allowing explicit cancelled/no-show status filters to include those records.
  - Aggregations should return creation-date, arrival-date, departure-date, status, source, payment, hotel, and room-type level summaries needed by the frontend charts.
  - Status labels may be humanized on the frontend, but backend values should remain normalized for filtering and reporting stability.
- Inventory/occupancy executive report backend behavior
  - The hotel-management overall inventory report must require one selected hotel before returning occupancy output.
  - Occupancy is calculated from arrival/check-in dates, not creation date or checkout date.
  - The month/range should be controlled by calendar type plus month/year selectors. Frontend may pass invCal, invHMonth/invHYear, invStart, and invEnd, but backend must compute and validate the effective range consistently.
  - Hijri is the default report calendar and should default to the current Hijri month when no valid month query exists.
  - includeCancelled/excludeCancelled must be honored consistently so the UI toggle matches the returned data.
  - The overall inventory endpoint should mirror the single-hotel inventory report structure while still using overall role and hotel-scope validation.
  - Avoid response/query feedback loops: do not mutate query dates from backend responses in a way that causes alternating invHMonth/invStart/invEnd requests.
- Paid reservations overview backend behavior
  - The paid overview returns hotel-level breakdown first, followed by the rest of the report data.
  - Payment summaries must distinguish paid amount, due amount, commission, and booking source/hotel grouping.
  - Data must stay scoped to assigned hotels unless the actor is a true super admin.
- Financial actions export behavior
  - GET /overall-dashboard/financial-actions/:userId supports exportAll=true for Excel export callers.
  - exportAll keeps the same role, owner, hotel, booking source, action type, date, and agent/order-taker scoping as the visible financial-actions page.
  - exportAll raises the reservation and wallet-claim limits up to a guarded maximum of 5000 rows per queue so the frontend can export the full filtered report without changing authorization logic.
  - Export tracking should continue through /overall-dashboard/financial-report-export/:userId with dataset=overall_financial_actions, including filters, hotel scope, row counts, reservation rows, and wallet transaction rows.
- Reservation summary modal and export tracking
  - Reservation list summary exports must create ActivityTracker records so report downloads are attributable.
  - Tracked metadata should include actor, owner scope, hotel scope, active filters, selected date field, row counts, and export/report type.
  - Summary aggregations include date, booking source, status, and room type. Source/status/room-type summaries should be sorted by total amount descending.
- B2B chat backend
  - Chat is B2B only and lives under the b2b-chat backend controller/routes/model/service structure.
  - Staff can chat internally with users allowed by their assigned hotel scope.
  - Hotel managers, Hotel System Admins, owners, finance users, and reservation users can chat with agents assigned to their hotels only.
  - Reception and housekeeping-style accounts should not gain agent chat access unless their role/scope explicitly permits it.
  - Owners and Hotel System Admins are powerful inside their assigned hotels but are not global chat users.
  - Super admins can view and participate across the platform.
  - Chat recipient lookup must filter by role and shared hotel assignment. A hotel owner should not see unrelated super admins or unrelated agents as start-new-chat recipients.
  - Chat recipient API responses should provide friendly roleLabel values such as Owner, Hotel System Admin, Hotel Manager, Finance, Reservations Officer, Agent, and Super Admin. Do not return numeric role codes as the display fallback.
  - Active chats are not closed chats. Historical chats are closed chats.
  - Chats with no activity for 2 hours should be marked seen and closed by the maintenance service. Shorter timeouts may be used only for local testing and then reverted.
  - Attachments and pasted screenshots are supported. Backend responses should expose enough metadata for inline preview while keeping safe download/open URLs available.
  - Unread counters and seen updates must not create request loops. Prefer stable polling/socket triggers and avoid marking a chat as seen merely because the top-navbar icon was clicked.
  - Socket events should refresh active chat lists and unread counters without rebinding listeners on every render.
  - Socket typing events use b2bTyping and b2bStopTyping rooms scoped to b2b-chat:<chatId>. They are transient relays only and should not write to MongoDB.
  - Frontend chat pages may receive legacy/null attachment rows from historical data. Backend should continue returning safe attachment metadata, but frontend must also guard null attachment entries before previewing kind/type/source fields.
  - The B2B chat route is an overall hotel-management surface, not a single-hotel surface. Top-navbar notification context should stay owner/overall scoped unless the user opens a true single-hotel route.
- Executive summary filter model
  - The overview, reservations overview, and paid reservations overview executive tabs may accept multi-hotel and multi-month filters for broad historical analysis.
  - These non-inventory executive tabs may send hotelId/invHotel as comma-separated lists, invMonths/invHMonth/invHYear/invCal, dateFrom/dateTo, invStart/invEnd, dateBy, status, search, and includeCancelled.
  - Backend report handlers must keep parsing repeated or comma-separated hotel/status/month params while enforcing accessible-hotel scope before aggregation.
  - dateFrom/dateTo and invStart/invEnd are authoritative date filters. If a legacy/shared URL contains date filters together with range=all, the effective report range must still be treated as a custom date range so database output and visible UI filters remain aligned.
  - Inventory remains stricter than the other executive tabs: exactly one hotel must be selected, and occupancy remains based on arrival/check-in date.
- Query, loop, and performance guardrails
  - Overall summary and inventory endpoints are expensive; frontend callers should debounce/stabilize query dependencies and backend should avoid unnecessary broad queries.
  - The summary endpoint should not be repeatedly called by the inventory report unless that tab truly needs summary data.
  - GET request logs alternating between different invHMonth/invStart/invEnd values usually indicate a frontend query synchronization loop, not a database issue.
  - For report work, always verify that selected filters, reflected UI labels, and backend results agree before considering the feature complete.
  - Backend report endpoints should stay idempotent: do not return response fields that force the frontend to rewrite query dates or range values repeatedly.
- 2026-05-25 backend file and route map
  - server.js mounts the backend route modules. Any new route must be registered here or through an already-mounted route file.
  - routes/overall_dashboard.js owns hotel-management overall dashboard/report endpoints.
  - controllers/overall_dashboard.js owns the owner/hotel scoped executive summary logic, reservation analytics, inventory/occupancy report data, paid overview data, account-management actions, and signup invitation handling.
  - controllers/reservations.js remains the single-hotel reservation workflow source and should stay aligned with overall reservation list filtering/sorting behavior.
  - routes/b2b_chat.js and controllers/b2b_chat.js own B2B chat access, recipient lookup, chat lists, messages, attachments, unread counters, seen state, and close-chat behavior.
  - models/b2b_chat.js stores B2B chat threads/messages/attachment metadata.
  - services/b2bChatMaintenance.js owns inactive-chat cleanup. Production inactivity should remain 2 hours after local testing.
  - services/activityTracker.js records account-management mutations and report/export activity, including reservation summary report downloads.
  - services/auditPrivacy.js must continue sanitizing privileged audit actor data before reservation/report responses are returned to non-super-admin viewers.
  - Frontend callers for the overall report routes are mainly hotels_frontend/src/HotelModule/apiAdmin.js and hotels_frontend/src/HotelModule/TheOverallStructure/OverallSummary/*.js.
  - Frontend callers for B2B chat are mainly hotels_frontend/src/HotelModule/B2BChat/* and hotels_frontend/src/HotelModule/AdminNavbar/TopNavbar.js.
  - If future logs show repeated OPTIONS/GET calls for chat or inventory, check frontend effect dependencies first, then verify that backend endpoints are idempotent and do not return query-normalization values that cause URL churn.

Recent Production Guardrails
- Reservation detail privacy, May 2026:
  - Non-super-admin viewers must never receive privileged admin actor names in reservation detail, pending queue, finance queue, report, or public-safe reservation responses.
  - When extending reservation workflow metadata, run the field through services/auditPrivacy.js if it contains a by/actor/admin/lastUpdatedBy style snapshot.
- Payment-state consistency, May 2026:
  - Backend payment fields may contain both a selected payment method and actual collected amounts. Do not infer a paid reservation from a method label alone.
  - Reconciliation and financial reports should use recorded/captured amounts and review statuses, not display labels, when deciding paid/due/rejected/eligible states.

Backend Change Checklist
- Start by identifying the route file, controller, model, and frontend caller for the feature.
- Check roleDescription, role, roles, roleDescriptions, accessTo, belongsToId, hotelIdWork, hotelIdsWork, hotelIdsOwner, and hotelsToSupport before changing authorization.
- When a true super admin receives 403, audit all helper layers for inconsistent checks. The expected platform-super-admin test is configured super-admin id OR numeric role 1000 OR normalized role description superadmin/super admin.
- Reuse existing helpers such as canCreateReservationForActor, getAccessibleOverallHotels, canAccessOverallSection, canManageHotelStaff, and reservation status helpers.
- Keep hotelId and ownerId values scoped to the authenticated user; do not rely on request params alone.
- Preserve Socket.IO notification behavior for reservations, housekeeping, support, and notification counters.
- Preserve ActivityTracker behavior for reservations, financial exports, and account management. When adding any account-management mutation, record actor, target account, changed fields, hotel scope, owner scope, and request metadata.
- When adding fields to models, update both create and update controllers, relevant report aggregations, imports/exports, and frontend API clients.
- When adding a route, place the route in ./routes and the business logic in ./controllers unless the local pattern clearly differs.
- When changing reservation or housekeeping status behavior, test owner, super admin, Hotel System Admin, hotel manager, reception, finance, order taker, reservation employee, housekeeping manager, and housekeeper scenarios.

Final Production Readiness Notes
- Overall reservation queries must remain scoped by actor role and assigned hotels. Approved agents/order takers may read their own overall pending/finance reservations only; hotel staff, managers, Hotel System Admins, and super admins keep their broader authorized queue visibility.
- Overall reservation list sorting is backend-supported for the frontend table controls. Keep sort keys such as booking_source, hotel, check-in, and checkout routed through controllers/overall_dashboard.js without bypassing hotel-scope or agent-scope filtering.
- Reservation sorting must support creation date in addition to booking date, source, arrival/check-in, and departure/check-out. Overall reservations use controllers/overall_dashboard.js; single-hotel reservation lists and pending-confirmation queues use controllers/reservations.js and must keep auth/hotel/agent scope checks before sorting.
- Overall reservation filters accept repeated or comma-separated hotelId, status, and bookingSource query params. Preserve parseQueryList behavior so frontend multi-select filters can send several values without bypassing hotel/role scoping.
- Overall reservation status filtering normalizes user-facing status names to lifecycle aliases before querying. Examples: Checked Out matches checked_out, early_checked_out, and closed; Confirmed also covers legacy ok; and state is only used as a fallback when reservation_status is missing/blank so old state values cannot pollute filtered results.
- Scorecard and chart drill-down requests from the overall summary must use the same actor/hotel/agent scoping as the visible reservation analytics. Do not add a shortcut endpoint that bypasses buildReservationMatch or getAccessibleOverallHotels.
- When a chart groups by a secondary date field, drill-down requests must keep the primary report date filter and add the secondary bucketDateBy/bucketDateFrom/bucketDateTo filter instead of replacing dateBy. This prevents a clicked bar from showing reservations outside the visible report basis.
- Financial and reservation notification routes must keep acknowledgement notifications separate from operational queue notifications. Ackable account/wallet/reservation decisions can disappear after click; pending reservation confirmation and pending financial actions must remain until the workflow state changes.
- Tracker privacy remains production-critical. Any admin/super-admin/hotel-system-admin audit actor details must be sanitized server-side for non-super-admin viewers before frontend rendering.
- For production readiness, run node --check server.js, node --check controllers/overall_dashboard.js, git diff --check, and a scoped manual test of signup invitation, activation, agent pending reservations, finance actions, notifications, and super-admin account updates.
- 2026-05-25 production validation: frontend and backend production HEADs were checked against origin; hotels-frontend and hotels-backend were online in PM2; executive summary reservation, inventory, paid-overview, B2B chat SPA routes returned 200; /api/currency-rates returned 200. Frontend runtime fixes were deployed without backend runtime changes.

2026-05-26 Thread Addendum - Backend Decisions, Data Rules, And References
- Purpose of this addendum
  - This section documents the latest hotel-management and admin-platform work from the full session so future changes do not accidentally undo the new behavior.
  - The work touched backend scoping, report data quality, support/B2B chat authorization, admin route security, and frontend-facing API expectations.
  - Frontend implementation references live in hotels_frontend/application_structure_frontend.txt under the matching 2026-05-26 addendum.

- Exact routes and business surfaces discussed
  - Hotel owner / hotel-management executive summary:
    - /hotel-management/main-dashboard?overall=summary&page=1
    - Important tabs: combined Hotels Summary, reservations overview, hotel inventory, and paid reservations report.
  - Hotel rooms map:
    - /hotel-management/main-dashboard?overall=hotel-map&page=1&mapHotelId=<hotelId>&mapOwnerId=<ownerId>
  - Admin platform dashboard and reports:
    - /admin/dashboard
    - /admin/jannatbooking-tools?tab=calculator
    - /admin/janat-website
    - /admin/customer-service
    - /admin/customer-service?tab=history-client-cases
    - /admin/overall-hotel-reports?tab=inventory
    - /admin/accounts-management
  - B2B chat:
    - /hotel-management/b2b-chat
    - Admin top-navbar chat dropdown links into the same B2B chat route with the exact chatId query when possible.

- Executive summary hotel table data quality
  - Occupancy in the hotel-summary table is displayed as occupied/totalRooms at the hotel level.
  - Blocked/out-of-service rooms must be excluded from room inventory and room availability denominators when they are not guest-sellable rooms.
  - In-house reservations remain occupied even if their scheduled checkout date has passed. A reservation still in-house is operationally occupied until it is actually checked out.
  - Cleaning/housekeeping summary is displayed as a fraction. The intended meaning is clean-or-available rooms versus dirty/unclean rooms, using room map/housekeeping state rather than a reservation-only shortcut.
  - Grand total rows must aggregate the same semantics as regular hotel rows. Do not sum display percentages; aggregate raw counts first and then format.
  - The "hotel path" column was removed from the summary table. Backend consumers should not rely on that value being displayed.

- Inventory/occupancy report data rules
  - Overall inventory report mirrors the single-hotel inventory logic.
  - It requires one selected hotel; broad all-hotel inventory maps are not expected in the occupancy grid.
  - Hijri calendar is the default calendar for the inventory tab and should default to the effective/current Hijri month when query state is missing.
  - Occupancy cells compare booked/occupied room nights against sellable capacity for the selected day and room-display group.
  - Admin inventory reports must match hotel-management inventory color and data logic. If they diverge, align the admin route to the hotel-management implementation.
  - The rightmost date/day column must remain visible and readable in RTL and LTR.
  - Payment-status and booking-source filters may be multi-select. Query handlers must keep parsing repeated and comma-separated params without bypassing authorization.
  - Modal day-details tables must show actual reservation rows behind a selected cell; style is frontend-side, but backend must keep returning reservation detail fields needed for guest, confirmation, phone, status, check-in, check-out, total, payment, source, count, and room type.

- Hotel rooms map data rules
  - The hotel rooms map is live/current state, not date-range based. It is expected to differ from inventory/occupancy reports that are date-range based.
  - Score cards on the rooms map should explain current state only: occupied/live, available, clean, dirty, under cleaning, and out-of-service/blocked.
  - Out-of-service/blocked pseudo rooms may exist in room maps and should be visible in the live map when useful, but they should not inflate sellable inventory in reports.
  - Room status filters must include clean versus dirty/unclean filtering; applying a clean/dirty filter must actually reduce the returned/displayed room set.
  - Tooltip hover behavior is frontend-side, but backend should keep returning enough room identity/status data so tooltips can be positioned away from clickable rooms.

- Super admin and platform admin separation
  - True platform super admins are configured through SUPER_ADMIN_ID in hotels_backend/.env. The value can contain comma-separated ids.
  - Configured super-admin ids are the source of truth for unrestricted platform-owner behavior.
  - Role 1000 alone is not enough to mean unrestricted platform owner. Role 1000 can also represent scoped platform employees. Always combine role/access/hotel-scope checks with configured super-admin checks.
  - Platform employees can work for the platform owner but may still be limited to specific admin routes, specific hotels, and specific customer/hotel conversations.
  - AdminRoute and backend requireAdminAccess should allow configured super admins across admin pages, while scoped platform employees must be restricted by accessTo and assigned hotels.
  - When a configured super admin enters a hotel-management route as a specific owner/staff context, data and mutations should behave as the dashboard user/context allows unless the route is explicitly a platform-admin route.
  - The /admin route family is the platform control panel. Keep its backend route authorization separate from hotel-management routes.

- Admin account management backend references
  - routes/admin_accounts.js and controllers/overall_dashboard.js/admin-account sections own platform/hotel account creation and updating.
  - models/user.js must remain the canonical place for account fields:
    - accountScope or equivalent platform/hotel distinction.
    - role, roles, roleDescription, roleDescriptions.
    - accessTo for admin feature links.
    - hotelIdWork, hotelIdsWork, hotelIdsOwner, hotelsToSupport, belongsToId for hotel scope.
  - Role labels must be friendly in responses and UI. Avoid showing numeric role codes such as 1000/2000 as the user-facing role.
  - Platform employee accounts require both route-level access and hotel/client scope. Do not treat every platform employee as a global super admin.
  - Admin accounts may create internal platform employees and hotel employees, but the backend must persist the right scope and permissions for each type.

- Support case backend updates and rules
  - routes/supportcase.js and controllers/supportcase.js own B2C/support-case chat and support case history.
  - services/supportCaseMaintenance.js auto-closes open client/B2C support cases after 1 hour of no activity.
  - Inactive B2C auto-close must never delete the SupportCase document or conversation; it only sets caseStatus=closed, closedAt, closedBy=csr, pauses AI, and preserves history.
  - Public/client message appends to an already closed B2C case are rejected so new activity cannot be hidden inside a closed history case.
  - Active and historical support cases must be scoped to the current actor:
    - Configured super admins can see all.
    - Platform employees see only cases allowed by accessTo and assigned hotels.
    - Hotel-side users see cases for their assigned hotels or cases assigned to them.
  - /support-cases/closed/clients supports pagination and newest-first sorting. It returns cases plus page, limit, total, pages, and sort metadata.
  - History client cases should be sorted newest to oldest by closedAt, updatedAt, createdAt, and _id fallback.
  - Support-case notification summaries must count only pending/unseen items visible to the actor.
  - Mark-all-seen and seen-by-admin operations must respect the same scope instead of globally clearing unrelated users' notifications.
  - deleteMessageFromConversation must check canSeeSupportCase before removing a message.
  - updateSupportCase sanitizes pushed conversation payloads and enforces sender display-name rules.

- Super-admin custom chat display name
  - Configured super admins may freely choose the sender display name shown in chat messages.
  - This is intentionally limited to configured SUPER_ADMIN_ID users, not all role 1000 users.
  - controllers/b2b_chat.js:
    - messageSenderNameForActor(actor, requestedName) accepts req.body.chatDisplayName or req.body.senderName only when isPlatformSuperAdmin(actor) is true.
    - Non-super-admin users are forced back to actor.name, actor.email, or Account.
  - controllers/supportcase.js:
    - enforceConversationSenderName(conversation, actor) accepts custom messageBy.customerName only for configured super admins.
    - Non-super-admin actor messages are forced back to actor.name, actor.email, or Account.
  - Names are trimmed, null-byte stripped, whitespace-normalized, and limited to 80 characters.
  - This backend enforcement is required even if the frontend hides the alias field from non-super-admins.

- B2B chat backend updates and rules
  - routes/b2b_chat.js, controllers/b2b_chat.js, models/b2b_chat.js, and services/b2bChatMaintenance.js own B2B chat.
  - Active B2B recipients should include active B2B-capable users only.
  - Configured super admins can speak to any active hotel manager or staff member across the platform.
  - Platform employees with role 1000 but no configured super-admin id must be scoped to their assigned hotels and allowed accessTo values.
  - Hotel owners, Hotel System Admins, managers, finance users, and reservation users remain hotel-scoped.
  - Team-side seen behavior:
    - For agent chats, when one team-side participant reads or takes over the chat, team-side unread notifications can be reduced for the relevant targeted team side.
    - Agent-side unread notifications must still depend on the agent participant's seen state.
  - Unread and seen endpoints must not create loops. Marking seen happens on explicit chat open/read, not merely by rendering a top-navbar dropdown.
  - Chat list and unread-summary endpoints must use visibleChatQueryForActor and unreadCountForActor so counts match what the actor can actually see.
  - emitChatUpdate broadcasts to b2b-chat:<chatId>, b2b-user:<userId>, b2b-platform, and b2b-hotel:<hotelId> rooms as appropriate.
  - Inactive chats are closed by services/b2bChatMaintenance.js after the configured production timeout, currently 2 hours.

- Notification backend behavior
  - Platform-admin top-navbar notification bell should summarize pending hotel notifications across all platform hotels for configured super admins.
  - Hotel-management top-navbar notifications stay scoped to the current owner/assigned hotel context.
  - Clicking a platform-admin notification should route the user into the exact hotel-management page where the pending action lives, not to a generic admin page.
  - Acknowledgement notifications can be deducted after action/ack; workflow notifications should remain until the underlying workflow state changes.
  - Chat notification dropdown should list active chats and route to the exact chatId when clicked.

- Server/runtime guardrails from this thread
  - server.js must keep one backend listener on port 8080. EADDRINUSE means another process is already using the port; do not start duplicate nodemon/server processes.
  - Socket listener setup must avoid repeated listener registration that causes MaxListenersExceededWarning. Watch for repeated server restarts and duplicate event binding.
  - CORS font errors from fonts.gstatic.com are frontend asset/font-loading issues; they are not backend route failures.
  - WebSocket connection errors often followed from the backend crash or EADDRINUSE, not an application infinite loop by themselves.

- Verification commands used during this session
  - Backend:
    - node --check controllers/b2b_chat.js
    - node --check controllers/supportcase.js
  - Frontend:
    - node --check src/AdminModule/AdminNavbar/AdminTopNavbar.js
    - node --check src/HotelModule/B2BChat/B2BChatMain.js
    - node --check src/AdminModule/CustomerService/ChatDetail.js
    - npm.cmd run build
  - Future backend validation should also include node --check server.js and route-specific API calls for support cases, B2B chat, admin accounts, and overall reports.

2026-05-26 Production Deployment Record
- Local Git publication
  - Backend repo: hotels_backend on master.
  - Commit pushed before deployment: 5778c93, message "Finalize admin platform and hotel management updates".
  - Remote: origin https://github.com/AhmedAbdelRazak/hotels_backend.git.
  - Command pattern used locally: git add ., git commit, git push -u origin master.
  - .env stayed ignored by .gitignore and was not committed.
  - GitHub reported existing Dependabot vulnerability counts after push. Treat those as follow-up dependency/security work, not as deployment blockers for this application-code push.
- Production server pull
  - SSH target: jannat.
  - Production directory: /home/ahmedadmin/Hotels/hotels_backend.
  - Pull command: git pull --ff-only origin master.
  - Production backend HEAD after pull: 5778c93.
  - Production working tree was clean before pull and clean after pull.
- Production validation and restart
  - Backend syntax checks on server:
    - node --check server.js
    - node --check controllers/b2b_chat.js
    - node --check controllers/supportcase.js
  - PM2 process restarted: hotels-backend.
  - PM2 status after restart: hotels-backend online.
  - Smoke API check:
    - curl http://localhost:8080/api/currency-rates returned HTTP 200 JSON.
  - Server log after restart included:
    - "Server is running on port 8080"
    - "MongoDB Atlas is connected"
    - "[socket] DB watcher broadcasting conversation updates enabled."
- Deployment notes
  - The deployment was a fast-forward pull; no server-side merge conflict occurred.
  - Existing UnauthorizedError entries in PM2 error logs are expected when protected routes are hit without an authorization token. They were not caused by the deployment itself.
  - Future deployments should repeat: local build/checks, commit/push, ssh jannat, git pull --ff-only, frontend build if frontend changed, PM2 restart, curl smoke checks, and documentation update.

Future TODO Backlog - Added 2026-05-26
- TODO 1: Executive summary data-quality audit and user-readability guide
  - Route to audit first:
    - /hotel-management/main-dashboard?ownerId=68b74714fb50e159d48c714d&overall=summary&page=1
  - Backend references:
    - controllers/overall_dashboard.js
    - routes/overall_dashboard.js
    - models/reservation.js or the active reservation model used by the route.
    - models/hotel_details.js for room-count, room-map, blocked/out-of-service, and roomCountDetails semantics.
  - Data-quality goals:
    - Verify hotel count, total rooms, available rooms, occupied fraction, clean/dirty fraction, reservations count, gross total, and blocked/out-of-service count against MongoDB.
    - Confirm blocked/out-of-service rooms are excluded from sellable capacity.
    - Confirm in-house reservations still count as occupied even after the planned checkout date until the operational checkout state changes.
    - Confirm the grand-total row aggregates raw values before formatting.
    - Confirm date filters, calendar filters, dateBy, status, hotelId/invHotel, and includeCancelled/no-show flags are reflected exactly in the query output.
  - User-readability goals:
    - Document how a hotel owner should read each score card and table fraction.
    - Add short, non-intrusive UI explanations/tooltips only where a number can be misunderstood, especially occupied/totalRooms and clean/dirty.
    - Avoid adding marketing copy or large help blocks to the operational report.
  - Acceptance criteria:
    - A written data-quality checklist exists for this route.
    - At least one real hotel, including Zad Al Safa, is manually reconciled against DB records and the UI output.
    - Any discrepancy is traced to either filter state, room-map state, reservation status, or backend aggregation logic before changing display code.

- TODO 2: Restructure the chatbot / AI agent properly, using SereneJannat as the reference pattern
  - Existing backend AI references:
    - aiagent/
    - aiagent/index.js
    - aiagent/core/*
    - aiagent/routes.js and aiagent/routes/index.js
    - routes/aiagent_learning.js
    - controllers/aiagent_learning.js
    - models/aiagent_learning.js
    - models/aiagent_learning_batch.js
  - Reference pattern to inspect before implementation:
    - SereneJannat setup on the home server or the corresponding repo/folder if available locally.
    - Compare folder boundaries, orchestration entry points, prompt/policy storage, learning flow, memory/state model, route shape, and deployment model before refactoring JannatBooking.
  - Restructure goals:
    - Separate chatbot transport, orchestration, policy, memory, tools/actions, learning, evaluation, and admin controls.
    - Avoid a single large prompt as the core behavior. The bot should retrieve and apply structured knowledge, policy, learned examples, and hotel-specific context.
    - Keep reservation creation/update actions guarded by deterministic validators before writing to reservations.
    - Keep human handoff and support-case visibility rules aligned with supportcase.js and B2B/chat access rules.
    - Keep logs/audit records for AI actions so production behavior can be reviewed.
  - Acceptance criteria:
    - A future AI module map exists before code moves.
    - Existing aiagent routes and support-case integration are not broken during refactor.
    - The final bot can explain why it answered or acted using retrieved policy/learning/tool context, not only a prompt blob.

- TODO 3: AI Learning Schema and dynamic learning behavior
  - Important note:
    - A first-pass learning schema already exists in models/aiagent_learning.js and models/aiagent_learning_batch.js. Future work should audit, improve, and migrate if needed rather than blindly creating duplicate models.
  - Schema goals:
    - Support hotel-specific and platform-wide learning.
    - Store source type, source case/reservation ids, anonymized examples, extracted intent, successful response pattern, decision rules, confidence/quality score, tags/topics, language, createdBy/reviewedBy, approval status, version, and timestamps.
    - Track negative examples and corrections, not only successful summaries.
    - Support batch summaries/playbooks while preserving the underlying case-level learnings.
  - Dynamic AI goals:
    - The AI should retrieve relevant approved learning records by hotel, language, topic, intent, and conversation context.
    - Prompt construction should be data-driven: policy + active hotel context + retrieved learning + deterministic tool results.
    - Admins should be able to review/approve/disable learning records before they affect production answers.
    - Learning rebuild jobs should be idempotent and safe to rerun.
  - Acceptance criteria:
    - Clear migration plan for existing AiAgentLearning and AiAgentLearningBatch collections.
    - Preview endpoint shows which learning records would influence a response.
    - Production answer path can run with learning enabled or disabled for safe rollout.

- TODO 4: Optional later - SSR public JannatBooking frontend support
  - Backend relevance:
    - SSR may require backend/API CORS, cookies, auth/session, cache-control, sitemap/metadata, and reverse-proxy considerations.
    - This is optional and should not disrupt the current hotels_backend production API.
    - Current public frontend reference directory: D:\JannatBooking\jannatbooking_frontend.
    - Proposed future SSR sibling directory: D:\JannatBooking\jannatbooking_ssr.
    - Backend API behavior should remain compatible with both the existing React app and the future SSR app during migration.
  - Frontend details live in hotels_frontend/application_structure_frontend.txt under the matching TODO.

- TODO 5: Chat authorization and visibility hardening
  - Goal:
    - A chat must only be visible to authorized participants and supervisors.
  - Required access model:
    - The two direct parties in the conversation can see and respond.
    - Hotel Managers and Hotel System Admins can see chats for hotels they are authorized to manage.
    - Jannat/XHotelPro SUPER ADMINS from the backend SUPER_ADMIN_ID environment list can see platform-wide chats.
    - Platform employees can only see chats permitted by their assigned route, hotel, client, and role scope. They must not inherit super-admin visibility.
  - Backend checks:
    - Audit all B2B and B2C chat list/detail/send/read/unread endpoints.
    - Enforce authorization server-side even if the frontend hides links or filters lists.
    - Ensure unread counters decrease only for users who are actually allowed to see and mark the conversation as read.
    - Ensure socket rooms/events are scoped to authorized user ids, hotel ids, and platform-super-admin rooms where appropriate.
    - Add regression tests or manual verification for hotel owner, hotel manager, hotel system admin, receptionist/agent/finance/housekeeping, platform employee, and SUPER ADMIN roles.
  - Acceptance criteria:
    - No user can fetch, subscribe to, or mark read a chat outside their allowed scope.
    - SUPER ADMIN can monitor all platform chats, but normal platform employees cannot.
    - Real-time notification counts match the authorized user's scope.

- TODO 6: Retire SendGrid and migrate secure outbound email
  - Goal:
    - Replace SendGrid with Nodemailer or another reputable email provider/library while keeping backend changes contained and secure.
  - Requirements:
    - Use API-key or token-based credentials from environment variables; never hardcode secrets.
    - Keep an email service abstraction so reservation, support, notification, and admin flows do not call a provider directly.
    - Preserve existing email templates, language handling, and business triggers unless intentionally improved.
    - Support secure production delivery with SPF, DKIM, and DMARC aligned to the sending domain.
    - Update Cloudflare DNS records for the new provider and remove obsolete SendGrid DNS records after the new path is verified.
    - Avoid open relay patterns, user-controlled From headers, header injection, and unsafe HTML/template rendering.
    - Add rate limiting, structured logging, and retry/error handling for outbound email failures.
  - Migration plan:
    - Inventory all current SendGrid imports/usages and templates.
    - Create a provider-neutral mail adapter.
    - Add the new provider using environment-based API credentials.
    - Test in sandbox/staging with DNS alignment before production switch.
    - Switch production gradually and monitor bounces, spam placement, and delivery logs.
    - Remove SendGrid credentials and DNS only after successful verification.
  - Acceptance criteria:
    - Existing email-triggering code changes minimally because it calls the mail adapter.
    - Production DNS passes SPF/DKIM/DMARC checks for the active sending domain.
    - SendGrid is no longer required after migration and cleanup.

- TODO 7: OTA inbound reliability and correctness audit
  - Goal:
    - Ensure OTA inbound reservations/updates/cancellations are processed correctly with no damage to existing PMS data.
  - Backend audit areas:
    - Identify every OTA inbound endpoint, webhook, parser, queue/job, and reservation mapping path.
    - Validate idempotency using OTA reservation ids, update ids, or provider event ids.
    - Confirm create/update/cancel/no-show flows map cleanly to the internal reservation schema.
    - Confirm room type, room count, guest count, dates, price, tax/fees, payment status, source/channel, confirmation number, and hotel owner/hotel id are mapped correctly.
    - Prevent duplicate reservations when providers retry webhooks.
    - Verify timezone/date-boundary handling, especially check-in/check-out and Hijri/Gregorian reporting implications.
    - Ensure inbound OTA data cannot bypass hotel/owner authorization or overwrite protected internal fields.
    - Log raw inbound payload references safely for traceability without exposing secrets or sensitive payment data.
  - Acceptance criteria:
    - OTA inbound test cases cover new booking, modified booking, cancellation, provider retry, and partial/invalid payloads.
    - Existing dashboards and reports remain consistent after OTA events.
    - Manual reconciliation can trace an OTA booking from inbound payload to reservation record to report totals.

2026-05-26 JWT Expired Session Handling Fix
- Problem observed:
  - Expired JWTs on protected routes produced noisy UnauthorizedError stack traces in the backend logs, for example:
    - /api/reservations/notifications/pending-confirmation/:userId
    - /api/hotel-details/executive-summary/:userId
    - /api/account-data/:accountId/:userId
    - /api/overall-dashboard/summary/:userId
- Backend fix:
  - File: server.js
  - Added a global Express error handler after route registration.
  - UnauthorizedError from express-jwt is now converted to a clean 401 JSON response:
    - code: TOKEN_EXPIRED
    - error: Session expired. Please sign in again.
  - Non-expired unauthorized JWT failures return:
    - code: UNAUTHORIZED
    - error: Unauthorized
- Security note:
  - Route protection was not weakened. The handler only normalizes the response and prevents stack-trace noise for expected authentication expiry.
- Frontend companion fix:
  - The matching frontend change clears expired stored JWTs and expired dashboard-preview tokens before API calls are made.

2026-05-26 Executive Summary Audit Completion
- User-facing issue investigated:
  - Route:
    - /hotel-management/main-dashboard?ownerId=68b74714fb50e159d48c714d&overall=summary&page=1&dateFrom=2026-03-27&invStart=2026-03-27&dateTo=2026-05-26&invEnd=2026-05-26&dateBy=createdAt&range=custom
  - Screenshot concern:
    - Zad Al Safa showed 7/56 in the occupied column, while the live room map appeared to show fewer current guests.
- Backend finding:
  - controllers/overall_dashboard.js computes summary occupancy as the peak occupied sellable rooms on any day inside the selected reporting range.
  - The live hotel room map uses current-day operational occupancy and can therefore show a lower number at the same moment.
  - For Zad Al Safa:
    - Hotel id: 68da202900a070e8123c27c4.
    - Owner id: 68b74714fb50e159d48c714d.
    - Physical rooms: 60.
    - Active/sellable rooms: 56.
    - Out-of-service rooms: 4.
    - Peak occupied rooms in 2026-03-27 through 2026-05-26: 7.
    - Peak dates: 2026-03-27 and 2026-03-28.
    - Current/live occupied rooms on 2026-05-26: 4.
    - Created-at reservation stats for the selected range: 0 reservations and 0 SAR for Zad Al Safa, matching the screenshot row.
  - Conclusion:
    - 7/56 is correct for the current executive-summary definition.
    - The old table label was ambiguous because it looked like live occupancy instead of range-peak occupancy.
- Documentation created:
  - audits/executive-summary-data-quality-2026-05-26.md
  - This audit is the source of truth for the Zad Al Safa reconciliation and the expected summary semantics.
- Future backend rule:
  - Do not change summary occupancy from range peak to live current state without a product decision.
  - If a future report needs current live occupancy, add a separate field such as currentOccupiedRooms instead of overloading occupiedRooms.
  - Keep blocked/out-of-service rooms excluded from sellable room capacity.
  - Keep in-house after planned checkout counted as occupied until operational checkout/room state changes.

2026-05-26 B2C Chat, AI Guardrails, And SEO Backend Alignment
- Reference studied:
  - D:\SereneJannat\serene_backend\controllers
  - D:\SereneJannat\serene_backend\services\supportChatOrchestrator.js
  - D:\SereneJannat\serene_ssr\src
- Important SereneJannat lesson:
  - B2C support automation should be guarded by per-case AI state.
  - Staff takeover should stop automation.
  - Customer-opened cases are the only automation target.
  - SSR metadata, JSON-LD, sitemap, and crawler support are valuable, but JannatBooking should not copy Serene's AI-crawler blocking behavior.
- Backend files changed:
  - models/supportcase.js
  - controllers/supportcase.js
  - routes/supportcase.js
  - server.js
  - aiagent/core/policy.js
  - aiagent/core/watcher.js
  - aiagent/core/orchestrator.js
  - aiagent/core/planner.js
  - aiagent/core/actions.js
- New SupportCase AI fields:
  - aiToRespond:
    - Per-case switch. This is the live source of truth for whether automation may respond.
    - Defaults to false.
  - aiResponderName:
    - Stable support name for the AI-assisted Jannat Booking support responder on a B2C case.
  - aiPausedAt:
    - Timestamp when automation was paused.
  - aiHandoffReason:
    - Reason code such as human_admin_message, manual_admin_toggle, reservation_cancellation, reservation_update, reservation_finalize, client_closed_case, or case_closed.
  - escalationStatus:
    - Separate B2C escalation lifecycle: none, active, addressed.
    - Active escalations are cases the AI or staff marked for personal Jannat Booking review.
    - Addressed escalations stay on the case audit trail but leave the active escalations workload.
  - escalationReason, escalationSource, escalatedAt, escalatedBy:
    - Explain why/how the case was escalated and by whom when applicable.
  - escalationAddressedAt, escalationAddressedBy, escalationAddressedNote:
    - Record when an employee marked the escalation as addressed.
  - humanTakeoverAt and humanTakeoverBy:
    - Admin/staff takeover audit fields.
- Public B2C support endpoints added:
  - GET /api/support-cases/client/:id
  - PUT /api/support-cases/client/:id
- Admin B2C escalation endpoint:
  - GET /api/support-cases-clients/escalated
  - Returns open client cases where escalationStatus=active, scoped by the same CustomerService visibility rules as other support-case admin endpoints.
- Public B2C endpoint rules:
  - Only openedBy=client support cases are exposed.
  - Public users can append sanitized customer messages.
  - Public users can close/rate their own client case.
  - Public users cannot change supporterId, hotelId, target user fields, admin assignment, AI fields, or staff metadata.
- Admin/staff support endpoint behavior:
  - PUT /api/support-cases/:id remains protected by CustomerService access.
  - Any authenticated admin/staff message on a B2C case sets aiToRespond=false.
  - The system records humanTakeoverAt, humanTakeoverBy, aiPausedAt, and aiHandoffReason=human_admin_message.
  - The socket room receives aiPaused so the public widget knows a human takeover occurred.
  - CustomerService staff can set escalationStatus=addressed after resolving an active escalation.
  - Closing an actively escalated B2C case also marks the escalation addressed so it leaves the escalated workload.
- B2B rule:
  - B2B chat remains owned by models/b2b_chat.js and controllers/b2b_chat.js.
  - B2B chats must not call aiagent, SupportCase AI fields, public B2C endpoints, or bot responder code.
  - Attempts to toggle AI on a non-client SupportCase are rejected.
- AI agent activation rule:
  - server.js only mounts aiagent when AI_AGENT_ENABLED=true.
  - aiagent/core/policy.js also checks:
    - global AI_AGENT_ENABLED=true or AI_FORCE_RESPOND=true.
    - supportCase.openedBy === "client".
    - supportCase.caseStatus === "open".
    - supportCase.aiToRespond === true.
    - hotel.activateHotel === true and hotel.xHotelProActive !== false unless force is explicitly enabled.
    - hotel.aiToRespond === true unless force is explicitly enabled.
  - This double guard is intentional. Do not remove one guard because the other exists.
- AI message identity:
  - AI-assisted support messages now use support@jannatbooking.com instead of management@xhotelpro.com.
  - Message userId is jannat-ai-support.
  - The visible name comes from the case's aiResponderName when present.
  - If asked directly, the assistant should be transparent that it is AI-assisted Jannat Booking support monitored by the team. It should not pretend to be a human.
- Human-only actions:
  - Cancellation and refund requests must stop automation and go to a human.
  - Existing reservation updates must stop automation and go to a human.
  - Final reservation commit/write is now human handoff in the AI planner, not automatic reservation creation.
  - The AI may help triage pricing by date range, hotel options near Al Haram, payment questions, and reservation context gathering.
  - Hotel recommendations and price quotes must read from active PMS/admin hotel inventory only:
    - activateHotel is the owner/manager activation gate.
    - xHotelProActive is the Jannat/XHotelPro platform activation gate.
    - Missing xHotelProActive on legacy hotel documents is treated as active until an admin explicitly sets it false.
    - aiagent/core/db.js listActivePublicHotels requires both gates and an active priced room.
- Hotel activation fields:
  - models/hotel_details.js stores activateHotel for hotel owner/manager activation.
  - models/hotel_details.js stores xHotelProActive for Jannat/XHotelPro platform activation.
  - Public hotel endpoints, public checkout guards, signup hotel filters, AI policy, and AI hotel recommendations must respect both gates.
- Documentation created:
  - audits/b2c-chat-and-seo-audit-2026-05-26.md
- Validation run after the change:
  - node --check controllers/supportcase.js
  - node --check models/supportcase.js
  - node --check aiagent/core/policy.js
  - node --check aiagent/core/orchestrator.js
  - node --check aiagent/core/watcher.js
  - node --check server.js
  - git diff --check in hotels_backend.
  - Frontend builds were also run in their repositories.
- Future backend follow-up:
  - Before enabling AI_AGENT_ENABLED in production, confirm OpenAI credentials, hotel-level aiToRespond opt-ins, admin monitoring workflow, and support escalation coverage.
  - Add API-level regression tests for:
    - B2B cannot trigger AI.
    - B2C client case can append via public endpoint.
    - Admin message pauses AI.
    - Cancellation text pauses AI.
    - Non-client support case rejects AI toggle.
  - If jannatbooking_ssr is built later, keep these public B2C endpoints compatible with both CRA and SSR clients.

2026-05-27 B2C AI Chat QA, Professional Tone, And Runtime Notes
- User-facing support tone:
  - Arabic B2C AI replies should address known clients respectfully as "أستاذ {first name}" when natural, for example "أستاذ ناصر".
  - The English operational shorthand for this rule is "Ostaz Nasser", but Arabic UI/chat output should render the Arabic title.
  - The tone should remain official, warm, concise, and human-like. Do not become stiff, generic, or overly long.
  - The brand must always be written exactly as "Jannat Booking"; do not shorten it and do not translate the brand.
- AI writer/orchestrator behavior:
  - aiagent/core/orchestrator.js now passes a respectfulAddress into every writer call.
  - The writer prompt instructs the LLM to reread the full conversation transcript before responding, avoid redundancy, and not ask for details already supplied.
  - The old "reason is not defined" save-path bug was fixed. The generated reply now saves after the typing delay instead of failing after the UI shows "typing".
  - The in-flight reply race was tightened so a newer client message can interrupt/cancel an older pending reply cleanly.
  - Payment guidance now passes known confirmation numbers into the writer so it does not ask again for a reference the client already supplied.
  - Hotel recommendation matches are title-cased before writing, so links render as names such as "Zad Al Sad" rather than lower-case hotel identifiers.
- AI scope and activation recap:
  - AI remains B2C only.
  - B2B chat must never trigger aiagent.
  - AI responds only when AI_AGENT_ENABLED=true and all Janat/global, support-case, hotel owner activation, platform activation, and hotel AI switches allow it.
  - AI_FORCE_RESPOND is local QA-only and should not be used as production behavior.
  - Admin/staff takeover pauses AI by setting aiToRespond=false and emitting aiPaused.
  - Closed cases and human-only reservation update/cancellation paths remove AI from the notification workload.
- Typing and notification QA:
  - The bot emits typing and stopTyping with isAi=true and the case aiResponderName.
  - Admin and public chat UIs should show the typing indicator during the human-like delay.
  - The public widget and admin ChatDetail should clear typing when receiveMessage arrives or stopTyping is emitted.
  - Chat notifications should disappear when the underlying case closes or when unseen counts are resolved by the proper seen endpoint.
- Long Arabic QA conversations run on 2026-05-26/2026-05-27:
  - Pricing/hotel-near-Al-Haram case:
    - Case 6a1634012f2ba647fbd0bdaf.
    - Confirmed Arabic replies, typing events, hotel links, and date-range price quote.
    - Quote observed for Zad Al Hayah double room, 2026-06-10 through 2026-06-12: 120 SAR total for 2 nights.
  - Payment case:
    - Case 6a1634542f2ba647fbd0bfd4.
    - Confirmed safe payment guidance, no card-data request, and follow-up no longer repeats the supplied confirmation JBQA777.
  - Reservation update/handoff case:
    - Case 6a1634942f2ba647fbd0c190.
    - Confirmed update request triggers human takeover and aiToRespond=false.
  - Hotel link retest:
    - Case 6a163538a7da89c44769e6f0.
    - Confirmed links show title-cased hotel names such as Zad Al Sad, Wardat Al Kheir, and Taaj Alzahabiya.
- Email notification resilience:
  - controllers/supportcase.js now treats SendGrid support-case email notifications as best-effort.
  - Once a SupportCase is saved, an email provider/network error is logged but does not turn the create endpoint into a 400.
  - This prevents duplicate client chats when SendGrid times out after MongoDB already saved the case.
- Local runtime note:
  - During QA, an extra background node server briefly occupied port 8080 and caused EADDRINUSE for nodemon.
  - The correct local posture is one backend process on port 8080. If EADDRINUSE appears, inspect Get-NetTCPConnection -LocalPort 8080 and stop only the extra server.js process, not frontend dev servers.
- Verification after these changes:
  - node --check aiagent/core/orchestrator.js.
  - node --check controllers/supportcase.js.
  - node --check hotels_frontend/src/AdminModule/CustomerService/ChatDetail.js.
  - npm.cmd run build in hotels_frontend compiled successfully.

2026-05-27 B2C Escalation Workflow
- Real escalation is now persisted on SupportCase, not just implied by chat wording.
- AI human handoff paths call updateSupportCaseAppend with:
  - aiToRespond=false.
  - aiPausedAt set.
  - aiHandoffReason set to the orchestrator reason.
  - escalationStatus=active.
  - escalationSource=ai.
  - escalationReason set from the orchestrator decision.
- The orchestrator decision schema includes human_escalation for requests outside available context or requiring human review.
- The handoff message is produced by the LLM writer after reading the full conversation, so wording is context-aware and multilingual.
- Escalation socket events:
  - supportCaseEscalated.
  - supportCaseEscalationAddressed.
  - supportCaseEscalationUpdated.
  - supportCaseUpdated remains the general sync event.
- Admin update behavior:
  - PUT /api/support-cases/:id with escalationStatus=addressed marks the escalation resolved.
  - If AI is restarted for a case, escalation fields are cleared back to none.
  - If an active escalated case is closed by staff or client, escalationStatus becomes addressed.
- Frontend route:
  - /admin/customer-service?tab=escalated-client-cases
  - Only open B2C cases with escalationStatus=active should appear in this tab.
  - Once addressed, the case should leave the escalated tab without closing the whole support case.
- Verification after escalation wiring:
  - node --check hotels_backend/controllers/supportcase.js.
  - node --check hotels_backend/routes/supportcase.js.
  - node --check hotels_backend/models/supportcase.js.
  - node --check hotels_backend/aiagent/core/orchestrator.js.
  - node --check hotels_frontend/src/AdminModule/CustomerService/ActiveClientsSupportCases.js.
  - node --check hotels_frontend/src/AdminModule/CustomerService/ChatDetail.js.
  - node --check hotels_frontend/src/AdminModule/CustomerService/CustomerServiceDetails.js.
  - node --check hotels_frontend/src/AdminModule/apiAdmin.js.
  - npm.cmd run build in hotels_frontend compiled successfully.

2026-05-27 B2C Chat QA Data Cleanup Record
- Purpose:
  - Clean chatbot QA trials before production-facing work continues.
  - Ensure no AI/chatbot test reservations remained in the PMS data.
- SupportCase cleanup:
  - Removed six client-opened, AI-related Arabic QA support cases from MongoDB.
  - Deleted case IDs:
    - 6a162ea65d1803768d198829
    - 6a1631155d1803768d198cce
    - 6a1634012f2ba647fbd0bdaf
    - 6a1634542f2ba647fbd0bfd4
    - 6a1634942f2ba647fbd0c190
    - 6a163538a7da89c44769e6f0
  - These cases were created for Arabic chatbot QA only and used names containing "اختبار".
- Reservation cleanup verification:
  - Explicit QA reservation marker count was 0.
  - Reservations created during the chatbot QA window, 2026-05-26 through 2026-05-28 UTC, was 0.
  - No reservation deletion was needed because the chatbot did not create reservations.
- Final verification after cleanup:
  - supportCaseQaLeftovers: 0.
  - explicitQaReservationCount: 0.
  - recentReservationCountInQaWindow: 0.
- Future QA cleanup rule:
  - Use explicit QA naming such as "اختبار", "QA Pass", or "JBQA" on support cases only.
  - Never use broad regex terms such as "QA" alone for reservations because real names can contain those letters.
  - Before deleting anything, run a read-only query and inspect the candidate IDs, dates, names, openedBy, and aiRelated fields.
  - Delete only targeted support-case IDs or unmistakable test reservations.

2026-05-27 Final Pre-Production Cleanup And Deployment Notes
- Final rude escalation QA cleanup:
  - Temporary case 6a16417650a34b197ce4d2ee was created only to verify the new escalated-cases tab.
  - It was removed before production deployment.
  - Final cleanup verification:
    - supportCaseQaLeftovers: 0.
    - explicitQaReservationCount: 0.
    - recentReservationCountInQaWindow: 0.
- No chatbot QA reservations were created or deleted.
- Deployment scope:
  - Backend B2C AI guardrails, escalation state, support-case cleanup docs, and related API changes.
  - PMS frontend customer-service AI controls, escalated-cases tab, notification/query-param behavior, and related docs.
  - Public frontend SEO/chat endpoint alignment and public chat docs.
  - jannatbooking_ssr remains documentation-only and is not yet a Git repository.
- Production deployment sequence:
  - Commit and push local Git repos to GitHub.
  - SSH target: jannat.
  - Pull backend in /home/ahmedadmin/Hotels/hotels_backend with git pull --ff-only origin master.
  - Pull PMS frontend in /home/ahmedadmin/Hotels/hotels_frontend with git pull --ff-only origin master.
  - Pull public frontend if a production checkout exists and it is tied to the same GitHub origin.
  - Build frontend apps that serve compiled assets.
  - Restart PM2 processes and run route/API smoke checks.
- Tomorrow follow-up action items:
  - Turn AI_AGENT_ENABLED on only after confirming production OpenAI key, Janat global AI switch, hotel AI switches, and support-team monitoring coverage.
  - Run one supervised production B2C test chat during a low-risk window, then remove the exact test support-case ID.
  - Decide whether to initialize jannatbooking_ssr as a Git repo or keep it as local planning documentation until implementation starts.
  - Add automated regression tests for support-case escalation transitions and B2C/B2B AI boundaries.

2026-05-27 Email Provider Rollback To SendGrid
- Summary:
  - We attempted to replace SendGrid with a home-server-controlled email path.
  - Outbound SMTP helper and OTA mailbox polling were implemented and tested in dry-run.
  - Outlook delivery to a real mailbox worked, but the home server could not authenticate to the Microsoft 365 mailbox over IMAP.
  - Microsoft returned:
    - 3 NO AUTHENTICATE failed.
  - Because the poller could not read the mailbox, no OTA email reached /api/ota/inbound/email and no reservation was saved from that path.
- Decision:
  - Roll back to the previous SendGrid-based implementation to protect production stability.
- Backend rollback:
  - Reverted the SendGrid-retirement commits.
  - Restored @sendgrid/mail dependency in package.json/package-lock.json.
  - Restored sgMail usage in:
    - controllers/auth.js.
    - controllers/braintree.js.
    - controllers/supportcase.js.
    - controllers/reservations.js.
    - controllers/janat.js.
    - controllers/paypal_reservation.js.
  - Restored SendGrid inbound route names:
    - GET /api/inbound/sendgrid.
    - GET /api/ota/inbound/sendgrid.
    - POST /api/inbound/sendgrid.
    - POST /api/ota/inbound/sendgrid.
  - Removed the temporary home-server mailbox poller and SMTP adapter files from the repository:
    - scripts/otaMailboxPoller.js.
    - services/emailService.js.
- Environment rollback:
  - Restored SENDGRID_API_KEY from the protected env backup.
  - Removed temporary OTA mailbox/Graph env values from local and production .env files.
  - Kept OTA_INBOUND_EMAIL_HOTEL_IDS because it existed before the experiment and remains part of OTA safety scoping.
- Safety verification:
  - No database records were deleted.
  - Read-only checks during the failed mailbox test showed no new InboundEmail record and no reservation created by the failed home-server path.
  - Production should continue using the existing SendGrid inbound DNS and SendGrid email sending until a future Microsoft Graph or dedicated mail-server plan is deliberately approved.
- Future note:
  - If this migration is retried, prefer Microsoft Graph application access over password-based IMAP, or keep SendGrid for inbound parsing.

2026-05-27 OTA Inbound Parser Hardening After SendGrid Rollback
- Active decision:
  - Keep SendGrid as the production email provider and inbound webhook path.
  - Do not use the failed home-server mailbox poller path for production OTA processing.
- Why this update was needed:
  - A forwarded Expedia test reached POST /api/inbound/sendgrid and saved an InboundEmail audit record.
  - The old deterministic parser misclassified the forwarded "New Booking" email as a reservation update because the body contained update/status language.
  - The old parser also missed table-style stay dates and could misread generic lines such as "Guest has PRE-PAID" as guest identity data.
- Backend changes:
  - services/otaReservationMapper.js now handles Expedia, Booking.com, Agoda, and Hotels.com provider detection more explicitly.
  - Strong "New Booking" / "New Reservation" / "Booking Confirmation" subjects take precedence over body-only update words.
  - Table-style OTA stay dates now parse month-first, day-first, and numeric date formats.
  - Table-style adults/children counts are extracted for room-capacity fallback matching.
  - Provider logo/header hotel names are cleaned so boilerplate such as "Hotel Conf" or image alt text is not used as the hotel name.
  - Generic guest parsing was tightened so payment boilerplate is not treated as a guest name.
  - If a provider labels an email as an update but no existing reservation matches, the backend can create a new reservation only when the email has a complete trusted OTA reservation payload.
  - Room matching now has a guarded occupancy fallback when the OTA room label is too different from the PMS room display names.
  - controllers/otaInbound.js duplicate blocking no longer treats needs_review or needs_mapping as final duplicate blockers, so previously failed OTA emails can be retried after parser or mapping fixes.
- services/otaEmailOrchestrator.js changes:
  - Added Hotels.com provider aliases.
  - Forwarded-email context can force a strong new-reservation classification from subject candidates.
  - Stale missing-field warnings are removed after fields are recovered by the parser/orchestrator.
- Local verification completed:
  - node --check services/otaReservationMapper.js.
  - node --check services/otaEmailOrchestrator.js.
  - Parser samples for Expedia, Booking.com, Agoda, and Hotels.com all returned new_reservation with provider, confirmation number, hotel, room, dates, guest, and guest-count fields populated.
  - Read-only MongoDB check resolved "Zad Al sad" to allowed hotel 68b74714fb50e159d48c714f and built a reservation document successfully.
  - Dry-run reconciliation with Reservations.create mocked confirmed that a complete update-labeled unmatched OTA email can proceed to created without writing to MongoDB during the test.
- Production deployment:
  - OTA parser hardening and retry-safety commits were pushed to origin/master and pulled into /home/ahmedadmin/Hotels/hotels_backend.
  - Production node --check passed for services/otaReservationMapper.js and services/otaEmailOrchestrator.js.
  - pm2 restart hotels-backend --update-env completed and hotels-backend is online.
  - GET http://127.0.0.1:8080/api/inbound/sendgrid returned the SendGrid inbound health response.
  - A production dry-run extraction returned Expedia/new_reservation with confirmation 245715336, hotel Zad Al sad, 2026-12-21 to 2026-12-25, guest name, and totalGuests 5.

2026-06-01 OTA Inbound Verification And Important Email Forwarding
- Business purpose:
  - OTA inbox addresses such as expedia-*/agoda-* at ota.inbound.jannatbooking.com may receive non-reservation emails that require human account control, such as email verification, one-time login codes, account-security alerts, or OTA action-required notices.
  - These emails should not be auto-clicked by the backend. The safe behavior is to forward them to an authorized human inbox while keeping the inbound audit record.
- Backend implementation:
  - services/inboundEmailForwarder.js classifies important non-reservation/security emails with deterministic rules and forwards them through SendGrid.
  - Default forwarding destination is ahmed.abdelrazak@jannatbooking.com.
  - Optional environment controls:
    - OTA_INBOUND_FORWARD_TO for comma-separated recipients.
    - OTA_SECURITY_FORWARD_TO as a fallback recipient variable.
    - OTA_INBOUND_FORWARD_FROM, default noreply@jannatbooking.com.
    - OTA_INBOUND_FORWARD_ENABLED=false/off disables forwarding.
    - OTA_INBOUND_FORWARD_DRY_RUN=true records a dry-run result without sending.
  - controllers/otaInbound.js runs forwarding after the inbound email has been parsed, audited, orchestrated, and reconciled.
  - Verification/security/action-required emails are forwarded even when the orchestrator classifies them as not_reservation.
  - OTA emails that land in needs_review, needs_mapping, or failed are forwarded when they appear to come from an OTA/provider context.
  - Normal successfully created/updated/cancelled OTA reservation emails are not forwarded by default to avoid noise.
- Audit model:
  - models/inbound_email.js stores forwardDecision and forwarding metadata, including reason, categories, link count, recipients, forwarding status, SendGrid message id, and errors.
  - Inbound email list API supports shouldForward and forwardingStatus query filters.
- Validation completed locally:
  - node --check controllers/otaInbound.js.
  - node --check services/inboundEmailForwarder.js.
  - node --check models/inbound_email.js.
  - Dry-run classification confirmed verification emails forward, normal created booking emails do not, and needs_mapping OTA emails forward for human review.

2026-05-28 Manual AI Chat Teaching For B2C Support
- Storage model:
  - models/aiagent_learning.js.
  - Manual uploads are stored in the aiagentlearnings collection with sourceType=manual_chat.
  - aiagentlearnings is the source of truth for newly uploaded teaching chats. Do not hardcode sample/training chats in backend responses, frontend modal state, or the orchestrator prompt.
  - Stores employee-uploaded external chat examples with:
    - chatTitle.
    - chatKeywords.
    - conversation array of {sequence, speakerName, role, message}.
    - summary, participants, customerIntent, supportResolution, learningNotes, responseGuidance, optional hotelId/hotelName, status, and createdBy.
- New controller:
  - controllers/aiagent_training_chats.js.
  - POST /api/aiagent-learning/chats accepts a pasted raw chat and uses OpenAI to clean spacing, fix obvious wording issues, infer client/support roles, produce keywords/title/guidance, and save the structured result.
  - If OpenAI is unavailable or model credentials fail, the endpoint falls back to deterministic parsing so the PMS can still save a usable training chat.
  - GET /api/aiagent-learning/chats lists active training chats for the admin history tab.
  - DELETE /api/aiagent-learning/chats/:id archives a training chat.
- Route protection:
  - Manual teaching-chat endpoints require requireSignin and CustomerService admin access.
  - Existing aiagent-learning build/selftest/debug endpoints remain unchanged.
- Chatbot orchestration:
  - OpenAI model selection is centralized in services/openaiModelConfig.js. The default model constant is gpt-5.5, and env values such as OPENAI_MODEL, OPENAI_MODEL_NLU, OPENAI_REASONING_MODEL, AI_ANALYSIS_MODEL, AI_MODEL, and OPENAI_DEFAULT_MODEL can override by use case.
  - OpenAI callers should use pickOpenAIModel/buildChatCompletionBody instead of hardcoded model names. The helper uses max_completion_tokens for GPT-5/o-series style models and max_tokens for older chat-completion models.
  - aiagent/core/db.js exposes listRelevantTrainingChats.
  - listRelevantTrainingChats reads active manual learning records across languages from aiagentlearnings and legacy aiagenttrainingchats; retrieval must not filter examples by the guest's preferred language.
  - The legacy aiagenttrainingchats collection is read only as backward-compatible learning context. New manual uploads and admin history records should go to aiagentlearnings/sourceType=manual_chat.
  - Teaching chats may be Arabic, Hindi, English, Spanish, or mixed language. The AI should learn the support behavior from all relevant languages.
  - aiagent/core/orchestrator.js now loads relevant employee learning examples inside the writer before each LLM-written reply.
  - The writer treats employee examples as private multilingual guidance only and must answer strictly in the support case preferredLanguage/preferredLanguageCode sent by the guest frontend.
  - controllers/supportcase.js keeps preferredLanguage/preferredLanguageCode on the SupportCase in sync with public/client message metadata, so jannatbooking_frontend remains the source for the guest's preferred response language.
  - Learning examples are passed as private context only; the guest should never be told that examples were consulted.
  - When a support case has a hotel, the writer now speaks as that hotel support/reservation desk and avoids presenting Jannat Booking as a separate middleman unless platform/payment/final verification context requires the brand.
  - Greetings, introductions, transcript labels, quote continuations, and human handoff messages now prefer the active hotel support voice when hotel context exists, so the guest does not feel passed through a platform middleman.
  - Legacy fallback replies for date/payment/reservation/amenity/handoff handling now pass through the same writer path where practical, so relevant training chats can shape tone and flow before a customer-facing answer is sent.
  - OpenAI calls now use bounded timeout/retry settings with an explicit 20s request deadline/no retry by default for live chat, and the orchestrator has deterministic decision/writer fallbacks so a slow model call does not leave the guest without a reply.
  - Complete booking requests that already contain dates and room type can go directly from deterministic NLU to quote writing, avoiding an extra planning model call.
  - English booking phrases with explicit room type and dates now have a deterministic NLU fast path, avoiding model calls for common messages such as double-room date-range requests.
  - Live-chat quote pricing now uses a bounded orchestrator-local pricing helper with date-loop guards and array checks, so a malformed pricing row cannot stall a chat turn.
  - The previous humanSend undefined reason failure was removed so normal replies can save after the typing delay.
- Validation completed locally:
  - node --check models/aiagent_learning.js.
  - node --check controllers/aiagent_training_chats.js.
  - node --check routes/aiagent_learning.js.
  - node --check aiagent/core/db.js.
  - node --check aiagent/core/orchestrator.js.

2026-05-29 Production B2C AI Chat Open Greeting Guard
- Root cause found during production monitoring:
  - A newly opened public B2C support chat initially contains only the system hold message.
  - aiagent/core/orchestrator.js joins the Socket.IO room and starts planTurn, but lastUserText is empty until the guest sends a message.
  - Without a proactive open-chat greeting, the widget can look idle even when the hotel/global AI switches are enabled.
- Backend behavior:
  - AI-enabled client cases with no customer message and no prior AI assistant reply now send one short hotel-support greeting on room join.
  - The greeting uses support-case preferredLanguage/preferredLanguageCode and the active hotel support voice.
  - The bot should not duplicate the greeting after refresh/rejoin because existing AI assistant replies are detected before sending.
  - humanSend re-checks ensureAIAllowed immediately before saving an AI message, so a delayed AI response is cancelled if the guest closes the case, staff pauses AI, the hotel switch is turned off, or the case is no longer open.
- Production note:
  - The hotel-level aiToRespond switch still controls whether public B2C AI runs for a hotel. If the UI shows "Chat Available" but aiToRespond=false, the chat is human reception support, not AI.

2026-05-29 AI Chat Multilingual Understanding And Hotel Scope Guard
- Production review:
  - The latest Amira/Zad Al Safa production chat showed a selected-hotel room question being answered with other hotel recommendations.
  - The guest then challenged the answer in Franko Arabic/Arabizi, and the AI correctly escalated to a human, but the earlier answer should have stayed scoped to Zad Al Safa.
- Backend behavior:
  - aiagent/core/nlu.js now tells the NLU model that guest messages may be native script, romanized/transliterated, code-switched, misspelled, or informal.
  - The NLU prompt explicitly covers Franko Arabic/Arabizi, Hinglish, Urdu/Hindi in Latin characters, Spanish/French without accents, and mixed-language writing as meaning-based input, not exact keyword input.
  - The NLU call passes active hotel room options as context so room-type mapping can use the selected hotel's real active room list.
  - Amenity detection can now come from the NLU model as well as the older deterministic fallback.
  - aiagent/core/orchestrator.js decision prompts now require a scope value: selected_hotel, alternative_hotels, platform, or null.
  - When an active hotel exists and the guest asks whether "you", "your hotel", or the selected hotel has rooms, amenities, availability, or pricing, the orchestrator must keep the turn scoped to that hotel.
  - Hotel recommendations are only used when the guest asks for other hotels, nearby alternatives, platform options, or when no hotel context exists.
  - The writer prompt repeats this selected-hotel rule so generated replies do not drift into cross-hotel suggestions unless alternatives are explicitly requested.
  - Selected-hotel room-type questions now answer from the active hotel room list first, then ask for dates if pricing/availability needs dates.
- Validation completed locally:
  - node --check aiagent/core/nlu.js.
  - node --check aiagent/core/orchestrator.js.
  - NLU smoke tests confirmed Arabic script and Franko Arabic room questions map to tripleRooms, Franko Arabic complaints remain other, and Hinglish/Spanish room requests map to tripleRooms.

2026-05-29 B2C AI Responder Name Rotation
- Backend behavior:
  - controllers/supportcase.js now rotates public B2C AI responder names per hotel instead of deterministically hashing the same guest/hotel into the same name every time.
  - The assigned aiResponderName is still saved on the SupportCase, so the visible assistant name remains stable for that one conversation.
  - The default responder pool is Aisha, Hana, Sara, Amira, Yasmin, and Nadia.
  - Production can override the pool without code changes using B2C_AI_RESPONDER_NAMES or AI_RESPONDER_NAMES as comma-separated names.
  - aiagent/core/orchestrator.js and the legacy aiagent/core/brain.js fallback also read the same environment-based pool when a case does not already have aiResponderName.

2026-05-29 Private Previous-Chat Preparation For B2C AI
- Backend behavior:
  - aiagent/core/db.js now exposes listPreviousGuestSupportChats for private same-guest support-case lookup.
  - Matching prefers stable guest identifiers from prior customer messages, such as messageBy.customerEmail or messageBy.userId. If no stable identifier is available yet, it falls back only to exact displayName1 within the same hotel.
  - models/supportcase.js includes supporting indexes for same-guest previous-chat lookup by openedBy/hotel/displayName and openedBy/conversation sender identifiers.
  - aiagent/core/orchestrator.js compacts up to four previous same-guest client support cases and passes them as privatePreviousGuestChats to the decision and writer prompts.
  - The previous-chat context is private operational memory only. The assistant must never tell the guest that old chats are visible, quote old chats, or reveal old private details unless the guest explicitly brings that detail into the current conversation.
  - The intended use is quiet preparedness: recurring preferences, unresolved issues, language style, and avoiding repeated questions.

2026-05-29 B2C AI Response Timing
- Backend behavior:
  - aiagent/core/orchestrator.js now makes human-like response timing configurable by environment variables instead of fixed constants.
  - Defaults reduce the hidden pre-typing delay while preserving the existing visible typing cadence:
    - AI_HUMAN_THINK_MIN_MS default 300.
    - AI_HUMAN_THINK_MAX_MS default 650.
    - AI_HUMAN_GREET_THINK_MS default 1200.
    - AI_HUMAN_TYPE_CHAR_MIN_MS default 48.
    - AI_HUMAN_TYPE_CHAR_MAX_MS default 60.
    - AI_HUMAN_TYPE_CLAMP_MIN_MS default 2200.
    - AI_HUMAN_TYPE_CLAMP_MAX_MS default 7000.
  - Normal short replies should start typing much faster; total delivery still depends on OpenAI latency and message length.

2026-05-29 Localized Phone Digit Normalization
- Backend behavior:
  - aiagent/core/nlu.js digitsToEnglish now normalizes Arabic-Indic, Eastern Arabic/Persian/Urdu, Devanagari/Hindi, Bengali, Gujarati, Tamil, Telugu, Kannada, Malayalam, Thai, Lao, and full-width digits to English digits.
  - This protects the AI reservation-detail phone capture step when guests type phone numbers in non-English digit scripts.
  - controllers/supportcase.js normalizes localized digits in public support-case contact identifiers before storing conversation sender email/phone values.
  - Phone-like contact identifiers are stored as English digits so SupportCase matching, previous-chat lookup, and downstream English-only systems stay consistent.

2026-05-29 Admin Support Chat Unread/Escalation Notification Rules
- controllers/supportcase.js:
  - getUnseenMessagesCountByAdmin and getSupportCaseNotificationSummary now count admin-unseen messages as real client/guest messages only.
  - AI/system messages are excluded from admin unread totals by isAi, isSystem, AI support sender emails, and the jannat-ai-support sender id.
  - The notification summary still returns activeEscalatedClientCases so the admin top navbar can mark escalated chats separately from normal active chat volume.
- Product behavior:
  - AI messages can still appear in the conversation transcript, but they should not inflate admin unread badges.
  - Escalated B2C cases remain open support cases until closed; resolving the escalation changes escalationStatus to addressed rather than closing the chat.
- Validation completed locally:
  - node --check controllers/supportcase.js.
- Commit and deployment:
  - Commit pushed to origin/master: 4d9c60d Refine support chat unread counts.
  - Production server /home/ahmedadmin/Hotels/hotels_backend pulled with git pull --ff-only.
  - Production validation: node --check controllers/supportcase.js passed.
  - pm2 restart hotels-backend --update-env completed and hotels-backend was online.
  - Health check returned 200 for http://127.0.0.1:8080/api/inbound/sendgrid.
  - Production logs showed /api/support-cases/notifications/summary returning 200 after restart.

2026-05-29 Housekeeping Actor Null Guard
- File:
  - controllers/housekeeping.js.
- Root cause:
  - Some housekeeping task requests can arrive without a resolvable active actor, causing userRoles(null) to throw "Cannot read properties of null (reading 'role')" in canManageHousekeepingHotel/listOfTasksForEmployee.
- Backend behavior:
  - userRoles and userRoleDescriptions now normalize null/undefined user objects to an empty object.
  - canManageHousekeepingHotel returns false immediately for missing or inactive actors.
  - The endpoint should now deny/return normally instead of writing a TypeError to the PM2 error log.
 - Validation completed locally:
  - node --check controllers/housekeeping.js.

2026-05-29 Support Case Client Identity And History Metrics
- models/supportcase.js:
  - SupportCase now has clientName, clientContact, and clientContactType for public/client B2C chats.
  - New cases store the normalized guest email/phone contact at creation time, and public guest messages refresh these fields when a real guest contact is present.
  - A supporting openedBy/clientContact/updatedAt index helps same-contact support history lookups.
- controllers/supportcase.js:
  - Client support-case list endpoints enrich B2C cases with clientProfile, caseTopic, and caseSubject before returning them to the admin UI.
  - clientProfile includes name, contact, contactType, totalChatsWithSameContact, and otherChatsWithSameContact.
  - Existing legacy cases are backfilled at response time from real guest conversation entries, while system/AI/support senders are ignored.
  - The history topic is derived from the case inquiry data instead of relying on a missing top-level inquiryAbout field.
- Validation completed locally:
  - node --check controllers/supportcase.js.
  - node --check models/supportcase.js.
  - Mongo smoke test inserted two exact Codex QA closed client cases with the same contact, verified caseTopic=reserve_room and totalChatsWithSameContact=2, then deleted those exact QA cases and verified zero leftovers.

2026-05-29 Admin Reservation Three-Price Breakdown
- Backend pricing model:
  - models/reservations.js now stores adminPricing and adminPricingVisibility as top-level reservation objects.
  - adminPricing summarizes clientTotal, rootTotal, netAfterExpensesTotal, otaExpenseTotal, and platformMarginTotal.
  - adminPricingVisibility.rootOnlyForHotelManagement marks reservations whose hotel-management view must show only root pricing.
- Pricing normalization:
  - services/reservationPricing.js now preserves the existing client-facing total_amount behavior while adding explicit per-day clientPrice/mainPrice, rootPrice, netAfterExpenses/netAfterOtaExpenses, otaExpenseAmount, and platformMargin values.
  - Existing price and totalPriceWithCommission remain the client/main price for backward compatibility with calendars, receipts, and older reservation screens.
  - summarizeRooms still returns total_amount as the client/admin total and sub_total as the root total, with adminPricing carrying the extra net/expense/margin totals.
- Admin-created and admin-updated reservations:
  - controllers/reservations.js flags new admin/order-taker priced reservations with adminPricingVisibility.rootOnlyForHotelManagement=true.
  - Admin reservation updates only receive the root-only hotel-management flag when pricing actually changes after server normalization.
  - adminPricing and adminPricingVisibility are server-managed on updates so the browser cannot force or spoof the visibility state.
- Hotel-management privacy:
  - services/auditPrivacy.js sanitizes flagged reservations for hotel-management/non-platform viewers by returning total_amount/sub_total as the root total, zeroing the visible commission, converting per-day room prices to rootPrice, and removing admin-only client/net/expense/margin fields.
  - Platform pricing viewers include super admin, admin/system admin, order taker, and reservation employee roles.
  - Non-platform users are blocked from changing dates, rooms, or pricing for admin-managed priced reservations to avoid overwriting hidden admin/client pricing with sanitized root-only data.
- Validation completed locally:
  - node --check services/reservationPricing.js.
  - node --check services/auditPrivacy.js.
  - node --check controllers/reservations.js.
  - node --check models/reservations.js.
  - Node smoke test verified client/root/net/expense/margin totals and root-only hotel-manager sanitization.

2026-05-29 AI Chatbot Learning Context And Dialect Handling
- aiagent/core/orchestrator.js:
  - Every writer response continues to receive the full current support-case transcript, private previous guest chat context, and relevant employee learning examples.
  - The learning lookup now includes the latest guest message, the recent/full conversation transcript, preferred language, ticket inquiry data, current wait state, slots, and action context.
  - The support-action decision step now also loads employee learning examples before it can choose human_escalation.
  - The decision prompt instructs the orchestrator to use reusable learning examples as a safe next step before escalating, while still escalating cancellations, refunds, mutations to existing reservations, or unsupported facts/tools.
  - The writer prompt now explicitly handles Arabic dialects including Egyptian/Gulf/Levantine/Maghrebi/Algerian/Tunisian, Franko Arabic/Arabizi, Hinglish, Urdu/Hindi Latin text, Spanish/French phonetic text, misspellings, and code-switching.
  - If the guest writes in a language different from the preferred language sent by the frontend, the bot must answer in the preferred language first and then ask one short language-switch question rather than silently switching languages.
  - The writer is constrained to known Jannat Booking routes/context URLs and should use concise markdown hotel links instead of inventing PMS/admin/payment links.
- aiagent/core/db.js:
  - listRelevantTrainingChats now reads all active AiAgentLearning records, including support_case and manual_chat sources, plus legacy AiAgentTrainingChat records.
  - Training relevance now searches the uploaded conversation turns themselves, not only summaries/keywords/guidance.
  - Token matching now preserves accented Latin, Arabic, and Devanagari script characters for multilingual learning search.
- aiagent/core/nlu.js:
  - The LLM fallback classifier was reinforced to infer hotel-support intent from dialectal, romanized, or mixed-script messages instead of classifying them as other.
- Validation completed locally:
  - node --check aiagent/core/orchestrator.js.
  - node --check aiagent/core/db.js.
  - node --check aiagent/core/nlu.js.

2026-05-29 Reservation Admin Workflow Privacy Hardening
- services/auditPrivacy.js:
  - Platform admin viewers now include configured super admins, role 1000, role 10000, and admin/platform/system admin role descriptions.
  - Privileged audit actors now include platform admins, order takers, reservation employees, and system admins by role number or role description.
  - Non-platform viewers have privileged admin/order-taker/reservation-employee audit entries removed from adminChangeLog and reservationAuditLog.
  - Related privileged workflow actor fields and timestamps are stripped for non-platform viewers so admin update steps do not appear in the reservation cycle.
  - Root-only hotel-management pricing remains limited to reservations flagged with adminPricingVisibility.rootOnlyForHotelManagement.
  - A public/client reservation sanitizer removes platform-admin workflow data and admin-only pricing fields while preserving client/main pricing.
- controllers/janat.js:
  - Public reservation lookup by confirmation now returns the public/client-safe reservation shape.
  - Public client reservation update responses also return the same client-safe reservation shape.
  - The admin all-reservations list uses the shared audit sanitizer with req.profile so platform admins keep full details and non-platform viewers do not see privileged workflow entries.
- Validation completed locally:
  - node --check services/auditPrivacy.js.
  - node --check controllers/janat.js.
  - Node smoke test verified hotel-management root-only sanitization, platform-admin full visibility, and public/client client-price-only sanitization.

2026-05-29 Admin Reservation Scorecard Filters
- controllers/janat.js:
  - Admin paginated reservation list now supports scorecard-driven filterType values for createdToday, createdThisWeek, and pendingConfirmation.
  - Existing payment/status filterType values remain unchanged for the manual filter buttons.
  - The scorecards payload now also includes pendingConfirmationReservations, notCapturedReservations, capturedReservations, notPaidReservations, and paidOfflineReservations.
  - These counts use the same formatted reservation/payment status values as the admin table so the clickable frontend scorecards and the table stay aligned.
  - Overall/payment/status scorecard counts are historical/current-filter-scope totals; createdToday and createdThisWeek remain created-date metrics.
- Validation target:
  - node --check controllers/janat.js.

2026-05-29 Platform Admin Reservation Status Overrides
- controllers/reservations.js:
  - Platform admins and super admins can still move closed reservations backward or reopen them through the reservation update endpoint.
  - A stricter configured-super-admin-only guard was added for force operational jumps from unconfirmed workflow states directly into in-house or checked-out statuses.
  - That force jump also requires the platform all-reservations status-update marker, so the exceptional check-in/check-out override remains tied to the /admin/all-reservations workflow.
  - Runtime-only admin override markers are stripped before persistence so they cannot be saved into reservation documents.
  - This protects hotel-management and regular admin flows while allowing configured super admins using the platform admin all-reservations workflow to correct exceptional lifecycle states.
- Validation target:
  - node --check controllers/reservations.js.

2026-05-29 Edit Reservation Modal Draft-State Follow-Up
- Backend/API status:
  - No backend schema, controller, or API change was required for the hotel-management edit reservation modal closing/refresh behavior.
 - The frontend now keeps edit changes in a local modal draft until updateSingleReservation succeeds, so the existing backend update endpoint continues to receive the same normalized reservation payload only on explicit save.
 - Inactive hotel access remains an edit-mode frontend selection concern; backend persistence behavior is unchanged.

2026-05-31 Pending Confirmation Inventory Brief
- controllers/hotel_inventory.js:
  - GET /api/hotel-inventory/:hotelId/availability now accepts includePendingConfirmation=true.
  - The default endpoint behavior is unchanged: pending-confirmation reservations are excluded from regular hotel-wide availability unless agent-scoped availability already requires them.
  - When includePendingConfirmation=true is supplied, pending-confirmation holds are counted in the current availability response for the requested stay range. This is used by PMS confirmation/review modals so staff can see what inventory is available now before accepting or financially approving a reservation.
  - Agent-scoped requests still require the agent override authorization and continue to use agentId as the private stock scope. Hotel-scoped finance and reservation employees may read this agent-scoped availability for their assigned hotel so confirmation and finance-review modals can show current inventory for the agent reservation being reviewed.
- Reservation inventory audit rule:
  - availabilitySnapshot remains the source of truth for what inventory looked like when the reservation was created.
  - Current availability must be loaded from the live hotel-inventory availability endpoint rather than recalculated in the frontend.
  - Confirmation and finance modals should display both values when available: booking-time before/after inventory from availabilitySnapshot and current available inventory for the same room displayName/roomType and date range.
- Frontend callers:
  - hotels_frontend/src/HotelModule/NewReservation/PendingReservationInventoryBrief.js renders the compact room inventory card.
  - The single-hotel pending confirmation modal, overall pending confirmation modal, and finance review modals request includePendingConfirmation=true so the displayed "available now" number reflects pending holds during staff review.
- Validation completed locally:
  - node --check controllers/hotel_inventory.js.
  - hotels_frontend npm.cmd run build compiled successfully.

2026-06-04 Public Checkout Inventory Availability Hotfix
- routes/hotel_inventory.js:
  - GET /api/hotel-inventory/:hotelId/availability uses optionalSignin, not requireSignin, so public JannatBooking checkout and hotel-page callers can load safe availability without a staff JWT.
  - Invalid Authorization headers must still return 401. Do not treat a bad token as an anonymous public request.
  - Public requests with agentId must return 401 because agent-scoped stock/pricing is private inventory.
- controllers/hotel_inventory.js:
  - Public/no-auth responses are sanitized to room_type, displayName, total_available, available, globalAvailable, start_date, end_date, pricingByDay date/price/calendarBlocked, blockedDates, calendarBlocked, and roomColor.
  - Public/no-auth responses must not expose reserved, occupied, rootPrice, commissionRate, totalPriceWithCommission, totalPriceWithoutCommission, basePrice, defaultRootPrice, agentPricingApplied, agentAssignedStock, agentReserved, agentOccupied, or agentInventoryApplied.
  - includeCancelled and includePendingConfirmation are honored only for authenticated PMS/admin/staff requests.
  - Authenticated PMS/admin/staff callers keep the full internal availability payload, including hotel cost/root price, commission, reserved/occupied counts, and authorized agent inventory fields.
- Production verification on 2026-06-04:
  - Public live URL for hotel 68bca932d0ce3198b51d0ba9 and 2026-06-05..2026-06-09 returned 200 with 5 rows and no private keys.
  - Fake Authorization and public agentId guard checks returned 401.
  - Authenticated JWT smoke test returned 200 and preserved internal fields.

2026-05-31 Agent Accounts, Payout Details, Wallet Claims, And Finance QA
- Business purpose:
  - External agents/order takers need to create reservations, maintain payout details for commission payment, submit wallet top-up claims, and see only their own agent-scoped dashboard context.
  - Owners, Hotel System Admins, hotel managers, finance users, authorized account-management users, and configured Super Admins need to see agent payout details where their hotel scope allows it.
  - Finance users need to approve wallet claims and mark unpaid reservation commissions as paid without breaking agent auditability.
- Role and scope expectations:
  - Agent accounts use role 7000 and roleDescription/roleDescriptions value ordertaker.
  - Agent accounts should normally have accessTo values newReservation and ownReservations.
  - Agent wallet and reservation queries are scoped through hotelIdWork, hotelIdsWork, hotelsToSupport, hotelIdsOwner, belongsToId, createdByUserId, orderTakeId, createdBy._id, and orderTaker._id.
  - Finance users can review wallet claims and mark commissions paid only within their hotel scope.
  - Finance and reservation employees may create external agent accounts where allowed, but they cannot activate/approve those agents unless owner/platform approval rules allow it.
  - Only configured platform Super Admins can preview/open another account's dashboard as that user. Normal owners, staff, finance users, reservation employees, and agents cannot access another user's dashboard as themselves.
- Agent invitation link backend flow:
  - Route: POST /api/overall-dashboard/signup-invitation/:userId.
  - Controller: controllers/overall_dashboard.js createSignupInvitation.
  - Creates a SignupInvitation record with a hashed invite code and a signed token.
  - Invitation payload may include accountType, hotelIds, name, email, phone, company fields, agentCommercialModel, agentOpeningWalletCredit, and applicationNotes.
  - Invitation payload must never include passwords, hashed_password, salt, or any credential field.
  - Route: GET /api/signup-invitation and GET /api/signup-invitation/:code.
  - Controller: controllers/auth.js resolveSignupInvitation.
  - Resolves invite code or token into a public-safe invitation response and validates invited hotel ids.
- Public invited agent signup backend flow:
  - Route: POST /api/signup.
  - Controller: controllers/auth.js signup.
  - Public agent/job applications require a valid invitation link.
  - Public agent signup creates an inactive ordertaker with agentApproval.status = pending and applicationReview.status = pending.
  - Public agent signup uses invited hotel scope and invited commercial model/opening wallet values when present.
- Immediate create-agent backend flow:
  - Route: POST /api/hotel-staff/create/:userId.
  - Controller: controllers/auth.js createHotelStaffUser.
  - Authorized owners/platform users can create an ordertaker directly.
  - If creatorCanApproveAgent is true, the agent is active immediately and agentApproval.status = approved.
  - If the creator cannot approve agents, the account remains inactive with pending approval and an agent_account_pending notification is emitted.
  - Finance/reservation users are constrained to external agent creation only when they use this path.
- Agent approval/update backend flow:
  - Route: PUT /api/hotel-staff/:staffId/:hotelId/:userId.
  - Controller: controllers/user.js updateHotelStaffUser.
  - Activation is controlled by canApproveAgentAccountsForOwner.
  - Approving an agent sets activeUser = true, agentApproval.status = approved, approvedAt, approvedBy, lastUpdatedAt, and lastUpdatedBy.
  - Public application approval also updates applicationReview.status = approved and approvedHotelIds.
  - Approval/update notifications should send users back to the overall account activation view, e.g. /hotel-management/main-dashboard?ownerId=<ownerId>&overall=activate-accounts&page=1&range=custom.
- Agent payout details backend flow:
  - Model: models/user.js agentPayoutDetails.
  - Self-update route: PUT /api/user/:userId.
  - Controller: controllers/user.js update.
  - Only users that look like agents/order takers can update their own agentPayoutDetails.
  - sanitizeAgentPayoutDetails stores country, preferredCurrency, preferredMethod, accountHolderName, bankName, bankCountry, iban, accountNumber, swiftCode, instapayAddress, instapayPhone, mobileWalletProvider, mobileWalletNumber, stcPayNumber, paypalEmail, and notes.
  - IBAN is trimmed, spaces are removed, and it is uppercased.
  - preferredCurrency and swiftCode are uppercased.
  - paypalEmail is normalized through the optional-email validator.
  - updatedAt and updatedBy are recorded inside the payout details object.
  - Authorized account updates can also set agentPayoutDetails through updateHotelStaffUser, but only for external agent accounts.
- Payout detail visibility:
  - Route: GET /api/overall-dashboard/accounts/:userId.
  - Controller: controllers/overall_dashboard.js overallAccounts.
  - Account-management responses include agentPayoutDetails for authorized owner/manager/Super Admin account views.
  - Route: GET /api/agent-wallet/summary/:userId and /api/agent-wallet/summary/:hotelId/:userId.
  - Controller: controllers/agent_wallet.js agentWalletSummary.
  - Wallet summary selects agentPayoutDetails and returns it under each summary's agent object for finance/owner views.
- Agent wallet claim backend flow:
  - Route: POST /api/agent-wallet/claims/:userId and /api/agent-wallet/claims/:hotelId/:userId.
  - Controller: controllers/agent_wallet.js createAgentWalletClaim.
  - Only the signed-in external agent can submit a wallet credit claim.
  - The endpoint derives the agent from req.auth/userId; it should not require the agent to type or select their own name.
  - Commission-only agents cannot submit wallet top-up claims.
  - Attachments are required, but pre-existing attachment objects with public_id and url can be accepted without uploading again.
  - Claim transactions are stored in AgentWallet with source = agent_claim, transactionType = deposit, status = pending, and reviewStatus = pending.
  - Agent wallet activities are tracked under category agent_wallet.
  - Wallet claim notifications are emitted to the relevant hotel/owner scope.
- Finance wallet claim review backend flow:
  - Route: PUT /api/agent-wallet/claims/:userId/:transactionId/review and /api/agent-wallet/claims/:hotelId/:userId/:transactionId/review.
  - Controller: controllers/agent_wallet.js reviewAgentWalletClaim.
  - Finance/manager/admin actors with wallet scope can approve or reject pending agent claims.
  - Approve changes status to posted and reviewStatus to approved, records reviewedAt, reviewedBy, and updatedBy.
  - Reject changes status/reviewStatus to rejected and requires rejectionReason.
  - Approved posted claims are included in walletAdded and balance calculations.
- Agent wallet todo backend flow:
  - Route: GET /api/agent-wallet/todos/:userId and /api/agent-wallet/todos/:hotelId/:userId.
  - Controller: controllers/agent_wallet.js agentTodoList.
  - Returns wallet_claim_pending / wallet_claim_rejected tasks plus reservation commission and pending-confirmation tasks for the requested agent.
  - The default requested agent is the signed-in agent when no agentId query is supplied.
- Commission-paid backend flow:
  - Route: PUT /api/reservations/commission-paid/:reservationId/:userId.
  - Controller: controllers/reservations.js markReservationCommissionPaid.
  - Finance, hotel managers, and admins can mark a scoped reservation commission as paid.
  - The endpoint sets commissionPaid = true, commissionStatus = commission paid, commissionPaidAt, commissionData when commission is supplied, and refreshes financial_cycle.
  - If the reservation belongs to an agent/order taker, buildCommissionAgentApprovalForFinanceUpdate creates commissionAgentApproval.status = pending so the agent can confirm/dispute the marked payment.
  - adminChangeLog and reservationAuditLog receive audit entries.
  - ActivityTracker receives a reservation status change from source commission_mark_paid.
- Super Admin dashboard preview backend safety:
  - controllers/user.js previewHotelStaffDashboard returns preview auth only to configured platform Super Admins.
  - controllers/auth.js requireSignin rejects preview JWTs unless previewActorId belongs to a configured Super Admin.
  - Reservation updates that include preview audit metadata only accept it when the authenticated actor is a configured Super Admin and the preview actor matches the request.
  - When preview auth is used on hotel-management routes, backend behavior should act as the previewed employee/agent for scoping and audit actor purposes, with preview metadata retained only for privileged auditing.
  - AdminModule /admin routes should not run as preview auth.
- 2026-05-31 finance actions, wallet review, and agent commission settlement:
  - controllers/overall_dashboard.js financial-actions response now includes three finance queues in one response:
    - reservations: reservation finance-action rows.
    - commissionReconciliation: checked-out agent reservations with unpaid/unreconciled commission for the selected commissionMonth.
    - walletClaims: pending wallet approval transactions.
  - The same endpoint also returns agentOptions so frontend filters can filter by agent/company across the finance queues.
  - commissionMonth is parsed as YYYY-MM and converted to a checkout_date range bounded by the current date for commission reconciliation.
  - Agent filtering uses the same ownership candidates as agent-scoped reservations: createdByUserId, orderTakeId, createdBy._id, orderTaker._id, agentWalletSnapshot.agent._id, agent._id, and agentId.
  - controllers/agent_wallet.js getAccessibleWalletHotels accepts ownerId from query so Super Admin/owner-scoped wallet views can be narrowed without reintroducing hotel-scoped wallet balances.
  - AgentWallet model/controller now stores rejectionType for wallet claims:
    - correction_required means the agent may correct/resubmit.
    - final means a total/permanent rejection and correctionAllowed is false.
  - Wallet claim review still requires a rejectionReason on reject and records reviewedAt, reviewedBy, updatedBy, reviewStatus, status, and ActivityTracker metadata.
  - Agent wallet todos and wallet notification feeds include rejectionType and correctionAllowed so the frontend can distinguish correctable rejection from final rejection.
  - controllers/reservations.js defines the finance settlement model helpers:
    - normalizeFinanceSettlementModel.
    - collectionModelFromFinanceSettlement.
    - agentSettlementModelForReservation.
  - Settlement model meanings are important:
    - source_commission_only: source commission only for non-agent reservations.
    - agent_commission_only: agent commission without wallet deduction.
    - agent_wallet_commission: wallet-backed agent reservation plus commission.
  - Agent commercial model meanings are important:
    - wallet_inventory means the agent has wallet-backed trust/stock behavior, but the agent still earns commission.
    - commission_only means no wallet backing, but the agent still earns commission.
    - mixed means finance/agent may choose wallet-backed plus commission or commission-only per reservation.
  - New agent/order-taker reservation creation:
    - create selects agentCommercialModel on the actor snapshot.
    - Force-pending agent reservations resolve an agentSettlementModel from the actor model and requested payload.
    - Default proposed commission rate is 8 when no rate is supplied.
    - Proposed commission amount is accepted from the payload if supplied; otherwise it is total_amount * rate / 100.
    - commissionData.assigned remains false and proposedByAgent is true.
    - financial_cycle.commissionAssigned remains false, commissionReviewStatus is pending, commissionType is percentage, and proposedByAgent is true.
    - walletRequired is true only for agent_wallet_commission.
    - collectionModel is agent_wallet for agent_wallet_commission and agent_commission_only for commission-only reservations.
  - buildCommissionMissingFilter treats proposed agent commissions as still needing finance review until commissionData.assigned or financial_cycle.commissionAssigned becomes true.
  - Finance review update flow in updatePendingConfirmationReservation:
    - Accept/reject finance fields are processed through the finance action path.
    - Rejection requires comments and supports financeRejectionType values total_amount, commission, and total_and_commission.
    - Rejection stores financeRejectionType, financeRejectionLabel, financeRejectionComment, amountReviewStatus, commissionReviewStatus, and commissionRejectionReason inside financial_cycle.
    - Rejection also updates agentDecisionSnapshot with the rejection type/label and sends a best-effort SendGrid email to the linked agent when the reservation has an agent email.
    - pendingConfirmationNotificationFeed emits agent_finance_rejected / finance_*_rejected notification items with rejection type, label, and comments so the agent dashboard has an action item.
  - Finance handling update flow:
    - financeSettlementModel/settlementModel/agentSettlementModel/sourceSettlementModel/collectionModel are accepted aliases.
    - The selected settlement model is validated against whether the reservation has an agent and against the agentCommercialModel.
    - Invalid combinations, such as source-only on an agent reservation or wallet handling for a commission-only agent, are rejected.
    - When an agent settlement model is saved, financial_cycle stores settlementModel, agentSettlementModel, collectionModel, walletDeductionRequired, commissionOnly, bookingSource, sourceName, and an agent snapshot.
    - For agent_wallet_commission, buildReservationAgentWalletSnapshot computes reservationWalletAmount = total_amount and balanceAfterReservation = balanceBeforeReservation - reservationWalletAmount.
    - For agent_commission_only or source_commission_only, wallet deduction is zero.
  - Commission reconciliation:
    - listCommissionReconciliationActions returns checked-out, unpaid agent commission rows inside the actor's hotel/agent scope.
    - The frontend marks selected rows by calling PUT /api/reservations/commission-paid/:reservationId/:userId.
    - Marking paid still creates commissionAgentApproval for agent-owned reservations, so the agent can approve/dispute the reconciled commission.
- 2026-05-31 local end-to-end QA completed:
  - Environment verified as local database host 127.0.0.1:27019 before mutating test data.
  - Created a Codex QA agent invitation through POST /api/overall-dashboard/signup-invitation/:ownerId.
  - Resolved the invite code through GET /api/signup-invitation/:code.
  - Confirmed invitation responses did not include password, hashed_password, or salt fields.
  - Created a fresh ordertaker agent through POST /api/hotel-staff/create/:ownerId.
  - Saved payout details as that agent through PUT /api/user/:agentId.
  - Confirmed owner account-management view can see the payout details through GET /api/overall-dashboard/accounts/:ownerId.
  - Confirmed finance wallet view can see payout details through GET /api/agent-wallet/summary/:financeId?agentId=<agentId>.
  - Confirmed opening wallet credit appears in agent wallet summary.
  - Submitted a wallet top-up claim as the agent through POST /api/agent-wallet/claims/:agentId without passing/selecting an agent name.
  - Confirmed the agent todo list shows wallet_claim_pending.
  - Approved the claim as a finance employee through PUT /api/agent-wallet/claims/:financeId/:transactionId/review.
  - Confirmed reviewedBy is the finance user and wallet balance increased.
  - Created a temporary agent-owned reservation directly in the local DB for commission testing.
  - Marked its commission paid as finance through PUT /api/reservations/commission-paid/:reservationId/:financeId.
  - Confirmed commissionPaid = true, commissionStatus = commission paid, and commissionAgentApproval.status = pending.
  - Confirmed the agent todo list receives a commission_agent_approval item.
  - Cleanup verified zero Codex QA users, zero Codex QA wallet transactions, zero Codex QA reservations, and zero Codex QA signup invitations remained.
  - Backend validation passed:
    - node --check controllers/auth.js
    - node --check controllers/user.js
    - node --check controllers/overall_dashboard.js
    - node --check controllers/agent_wallet.js
    - node --check controllers/reservations.js
    - node --check services/activityTracker.js
  - Frontend validation passed:
    - hotels_frontend npm.cmd run build compiled successfully.
- 2026-05-31 final documentation-pass validation:
  - node -c controllers/reservations.js passed after the final agent wallet/commission clarification.
  - hotels_frontend npm.cmd run build passed after the final agent wallet/commission clarification.
  - No additional live QA reservations were created during this documentation pass.

2026-06-01 OTA Calendar Orchestrator Preparation
- Business purpose:
  - Super Admins need a low-cost way to prepare calendar/rate/availability changes for Expedia, Agoda, Airbnb, and Booking.com from the PMS calendar screen.
  - The first implementation is deliberately a supervised preparation layer, not an unattended scraper or channel-manager replacement.
- Backend files:
  - routes/ota_calendar.js.
  - controllers/ota_calendar.js.
  - services/otaCalendarOrchestrator.js.
  - models/ota_calendar_job.js.
- API:
  - GET /api/ota-calendar/options/:userId returns supported OTAs and credential-readiness metadata for Super Admins.
  - POST /api/ota-calendar/jobs/:userId/prepare creates an OtaCalendarJob for a hotel room/date range.
  - GET /api/ota-calendar/jobs/:userId/:jobId returns a prepared job.
- Authorization and safety:
  - Only Super Admin actors may use the endpoint. The controller checks configured Super Admin ids, role 1000, and superadmin-style role descriptions.
  - Payloads are restricted to calendar/rate/availability fields only.
  - Payloads must include confirmSupervisedOnly: true, so callers explicitly acknowledge that this is not an unattended OTA submitter.
  - Passwords, secrets, tokens, cookies, and session data are rejected in API payloads.
  - OTA password lookup is by environment reference only: OTA_PASSWORD.
  - Optional OTA username env vars: OTA_EXPEDIA_USERNAME, OTA_AGODA_USERNAME, OTA_AIRBNB_USERNAME, OTA_BOOKING_USERNAME.
  - Optional dashboard URL env vars: OTA_EXPEDIA_DASHBOARD_URL, OTA_AGODA_DASHBOARD_URL, OTA_AIRBNB_DASHBOARD_URL, OTA_BOOKING_DASHBOARD_URL.
  - The saved job policy states no captcha bypass, no guest/payment/reservation access, human verification required, and manual final submit required.
- Job behavior:
  - Supported OTAs are expedia, agoda, airbnb, and booking.
  - Jobs store dateFrom/dateTo, nightlyRateSar, rootRateSar, availability, closed state, selected OTAs, per-day calendar rows, credential readiness, and supervised task steps.
  - Missing usernames or missing OTA_PASSWORD creates status needs_credentials; otherwise status is prepared.
  - This endpoint prepares a supervised calendar task. It does not currently log into OTA dashboards or submit changes.

2026-06-01 Overall Settings Room Manager And Calendar Pricing Backend
- Business purpose:
  - The Overall Settings screen now has separate backend endpoints for bulk room management and bulk calendar pricing.
  - These endpoints were added without changing the existing single-hotel settings routes, so the older stable setup remains intact.
  - Both workflows write to the existing HotelDetails.roomCountDetails structure.
- Backend files:
  - routes/overall_dashboard.js wires the new GET/POST routes.
  - controllers/overall_dashboard.js owns option loading, validation, schema-safe merging, and saves.
  - services/overallRoomTextOrchestrator.js handles bilingual room name/description orchestration.
  - services/overallCalendarPricingOrchestrator.js builds pricing plans and calendar rows.
  - services/openaiModelConfig.js is used by the room text orchestrator when OpenAI is configured.
- API routes:
  - GET /api/overall-dashboard/settings-room-manager/:userId.
  - POST /api/overall-dashboard/settings-room-manager/:userId.
  - GET /api/overall-dashboard/settings-calendar-pricing/:userId.
  - POST /api/overall-dashboard/settings-calendar-pricing/:userId.
  - All four routes use requireSignin, isAuth, and router.param("userId", userById).
  - Controllers call requireOverallSection(req, res, "settings"), so access is scoped to the hotels visible in the Overall Settings section and the ownerId query parameter.
- Room manager options endpoint:
  - Handler: overallRoomManagerOptions.
  - Returns the accessible hotels and their roomCountDetails in a frontend-friendly shape.
  - Response is used by src/HotelModule/TheOverallStructure/OverallSettings/OverallRoomManagerModal.js.
- Room manager save endpoint:
  - Handler: saveOverallRoomManagerRoom.
  - Accepts action add or update.
  - Add mode accepts multiple hotelIds so the same room can be created across several hotels in one operation.
  - Add mode supports per-hotel counts through the submitted roomCounts map/array.
  - Update mode targets one hotel and one roomId.
  - The endpoint validates that every requested hotel belongs to the actor's allowed Overall Settings scope.
  - Duplicate room protection checks same roomType/displayName combination inside each target hotel.
  - Existing room arrays are reused; no new top-level room collection is introduced.
  - New rooms are appended to hotel.roomCountDetails with default arrays for pricedExtras, pricingRate, agentInventory, agentPricingRate, photos, offers, and monthly.
  - overallRoomsCount is recalculated from roomCountDetails after add/update.
  - Shared men/women rooms stay in the existing structure by using the existing room type plus roomForGender instead of adding a new schema path.
- Room text AI orchestration:
  - Service: overallRoomTextOrchestrator.orchestrateRoomText.
  - The user submits one room name and one description.
  - If OpenAI is configured, the service creates polished English and Arabic values:
    - displayName.
    - displayName_OtherLanguage.
    - description.
    - description_OtherLanguage.
  - The service also infers bedsCount from explicit text when possible, but the UI does not require the user to enter a bed count.
  - OpenAI key lookup supports OPENAI_API_KEY and CHATGPT_API_TOKEN.
  - If no key is configured, the key is invalid, or the model call fails, the endpoint still saves fallback text so the user is not blocked.
  - The API response includes ai.applied, ai.model, ai.skippedReason, and ai.error for diagnostics.
- Calendar pricing options endpoint:
  - Handler: overallCalendarPricingOptions.
  - Returns accessible hotels, rooms, and approved/active order taker agents related to those hotels.
  - Rooms include roomType, displayName, displayName_OtherLanguage, roomForGender, count, activeRoom, roomColor, pricingDays, and agentPricingDays.
  - Agent options are filtered by hotel assignments and active/approved account state.
  - Response is used by src/HotelModule/TheOverallStructure/OverallSettings/OverallCalendarPricingModal.js.
- Calendar pricing save endpoint:
  - Handler: saveOverallCalendarPricing.
  - Accepts scope general or agents.
  - General scope writes to roomCountDetails[n].pricingRate.
  - Agents scope writes to roomCountDetails[n].agentPricingRate.
  - The endpoint requires at least one hotel and one room selection.
  - Agent scope also requires at least one selected agent.
  - Every hotel, room, and agent is validated against the actor's allowed Overall Settings scope.
  - Bulk mode accepts dates, roomSelections, sellingPrice, commissionPercent, status, and calendarType.
  - Explicit row mode accepts rows with hotelId, roomId, calendarDate, status, sellingPrice, and commissionPercent; this is what the preview modal sends after row-level edits.
  - The maximum generated payload is protected by services/overallCalendarPricingOrchestrator.ensurePlanSize, currently capped at 25000 generated rows.
- Calendar pricing plan rules:
  - Service: overallCalendarPricingOrchestrator.buildPricingPlan.
  - Selling price is the PMS user-facing nightly price.
  - Commission percent is converted to the room/calendar root price:
    - rootPrice = sellingPrice - (sellingPrice * commissionPercent / 100).
  - Saved calendar rows store:
    - price = sellingPrice.
    - rootPrice = calculated root price.
    - sellingPrice = sellingPrice.
    - commissionPercent = commission percent.
    - commissionRate = 0.
  - commissionRate is intentionally saved as 0 so the existing PMS calendar/reservation logic does not add the commission a second time.
  - Blocked/closed/restricted rows save selling price, root price, and commission as 0 and use black as the row color.
  - Non-blocked rows receive a deterministic color based on scope, selling price, and commission.
- General calendar fallback pricing:
  - General Calendar Pricing saves also update the selected room's fallback price fields.
  - roomCountDetails[n].price.basePrice is set to the top-level selling price per night.
  - roomCountDetails[n].defaultCost is set to the calculated top-level nightly root price.
  - This ensures a general fallback price exists when no calendar row is available for a date.
  - Agent Calendar Pricing does not update these public/general fallback fields.
  - If explicit preview-row mode has no usable top-level price, the backend can fall back to the first open explicit row, but the frontend normally submits the top-level selling price per night and commission.
- Calendar merge and overwrite behavior:
  - General calendar rows are merged by room + calendarDate.
  - Saving a full month replaces only submitted dates for the selected rooms.
  - Saving a smaller range, such as July 7 to July 20, replaces only those submitted dates and leaves the other dates unchanged.
  - Agent calendar rows are merged by agent + room + calendarDate.
  - Updating Agent A for a room/date range does not overwrite Agent B for the same room/date range.
  - Updating a later version of July after a previous July save will overwrite only the dates included in the new request.
- Calendar row builders:
  - buildGeneralRow writes public/general pricingRate rows with calendarDate, room_type, displayName, price, rootPrice, commissionRate, sellingPrice, commissionPercent, color, calendarType, source, and blocked status when relevant.
  - buildAgentRow adds agentId, agentName, agentEmail, and companyName on top of the general row shape.
  - agentSnapshot normalizes agent metadata for agentPricingRate rows.
- Validation performed during implementation:
  - node --check controllers/overall_dashboard.js passed.
  - node --check services/overallCalendarPricingOrchestrator.js passed.
  - Frontend build passed after the related UI/API wiring.
  - Local smoke tests created temporary Codex-marked owner/hotel/agent/calendar documents only, verified general and agent calendar saves, verified blocked row behavior, verified fallback basePrice/defaultCost updates, and then deleted only those temporary test documents.
  - Cleanup was verified with zero remaining Codex test documents for those smoke runs.

2026-06-03 Admin Global Hotel Settings Backend
- Business purpose:
  - Platform admins and SUPER admins have a separate admin-global settings workflow for all hotels across the platform.
  - This is not an owner-scoped or hotel-management Overall Settings route. Do not route admin-global callers through /api/overall-dashboard/settings*, and do not rely on ownerId query scoping for this workflow.
  - The admin-global workflow writes to the same existing HotelDetails.roomCountDetails structure used by regular hotel settings so room/calendar data stays compatible with reservation, inventory, OTA, and public hotel flows.
- Backend files:
  - routes/admin_global_hotel_settings.js wires the admin-global GET/POST routes.
  - controllers/admin_global_hotel_settings.js owns all option loading, validation, schema-safe merging, and saves for this admin-global workflow.
  - services/overallRoomTextOrchestrator.js handles bilingual room name/description orchestration.
  - services/overallCalendarPricingOrchestrator.js builds pricing plans and calendar rows.
- Authorization:
  - All routes use requireSignin, isAuth, requireAdminAccess("AdminDashboard", "HotelReports"), and router.param("userId", userById).
  - Configured SUPER admins pass through requireAdminAccess.
  - Role 1000 platform admins must have AdminDashboard or HotelReports in accessTo.
  - Hotel owners, Hotel System Admins, hotel managers, reservation employees, finance users, order takers, and other hotel-management users should not receive these admin-global routes unless they are also true platform admins with the required access.
- API routes:
  - GET /api/admin/global-hotel-settings/overview/:userId.
  - GET /api/admin/global-hotel-settings/room-manager/:userId.
  - POST /api/admin/global-hotel-settings/room-manager/:userId.
  - GET /api/admin/global-hotel-settings/calendar-pricing/:userId.
  - POST /api/admin/global-hotel-settings/calendar-pricing/:userId.
- Overview endpoint:
  - Handler: adminGlobalHotelSettingsOverview.
  - Returns every hotel sorted by hotelName with owner metadata, setup readiness, activation flags, room counts, room type count, photo count, and timestamps.
  - Response is used by src/AdminModule/GlobalHotelSettings/AdminGlobalHotelSettingsMain.js.
- Room manager options endpoint:
  - Handler: adminGlobalRoomManagerOptions.
  - Returns all hotels and their roomCountDetails in a frontend-friendly shape for admin room add/update workflows.
  - Response is used by src/AdminModule/GlobalHotelSettings/AdminGlobalRoomManagerModal.js.
- Room manager save endpoint:
  - Handler: saveAdminGlobalRoomManagerRoom.
  - Accepts action add or update.
  - Add mode accepts multiple hotelIds so the same room definition can be created across multiple hotels in one operation.
  - Add mode supports per-hotel room counts through countsByHotelId / hotelCounts style maps.
  - Update mode targets one hotelId and one roomId.
  - Every hotel id must be a valid HotelDetails id, but no ownerId scope filter is applied because this route is platform-admin global.
  - Duplicate room protection checks same roomType/displayName or roomType/displayName_OtherLanguage combination inside each target hotel.
  - New rooms are appended to hotel.roomCountDetails with default arrays for pricedExtras, pricingRate, agentInventory, agentPricingRate, photos, offers, and monthly.
  - overallRoomsCount is recalculated from roomCountDetails after add/update.
  - Shared men/women rooms stay in the existing structure by using individualBed plus roomForGender instead of adding a new schema path.
- Calendar pricing options endpoint:
  - Handler: adminGlobalCalendarPricingOptions.
  - Returns all hotels, active rooms, and active/approved order-taker agents related to those hotels.
  - Agent options are derived from hotelIdWork, hotelIdsWork, hotelsToSupport, and hotelIdsOwner.
  - Response is used by src/AdminModule/GlobalHotelSettings/AdminGlobalCalendarPricingModal.js.
- Calendar pricing save endpoint:
  - Handler: saveAdminGlobalCalendarPricing.
  - Accepts scope general or agents.
  - General scope writes to roomCountDetails[n].pricingRate.
  - Agents scope writes to roomCountDetails[n].agentPricingRate.
  - The endpoint requires at least one hotel and one room selection.
  - Agent scope also requires at least one selected agent.
  - Every selected hotel and room is validated against real HotelDetails records; every selected agent must match the active/approved agent query for the selected hotel ids.
  - Bulk mode accepts dates, roomSelections, sellingPrice, commissionPercent, status, and calendarType.
  - Explicit row mode accepts rows with hotelId, roomId, calendarDate, status, sellingPrice, and commissionPercent; this is what the admin preview modal sends after row-level edits.
  - The maximum generated payload is protected by services/overallCalendarPricingOrchestrator.ensurePlanSize, currently capped at 25000 generated rows.
  - General calendar saves also update the selected room fallback price fields: price.basePrice receives the selling price, and defaultCost receives the calculated root price.
  - Agent calendar saves do not update public/general fallback room price fields.
- Calendar merge behavior:
  - General calendar rows are merged by room + calendarDate.
  - Agent calendar rows are merged by agent + room + calendarDate.
  - Saving a range or month replaces only the submitted dates for the selected rooms/agents and leaves other dates untouched.
  - Blocked rows save price/rootPrice/sellingPrice/commission as 0 and mark status=blocked, blocked=true.
- Validation performed during implementation:
  - node --check server.js passed.
  - node --check controllers/admin_global_hotel_settings.js passed.
  - node --check routes/admin_global_hotel_settings.js passed.
  - Route module import smoke check passed with local dummy JWT/SENDGRID env values.
  - hotels_frontend npm.cmd run build compiled successfully after the related admin UI/API wiring.

2026-06-02 Reservation Team Reject/Cancel And Agent Update Restrictions
- Backend files:
  - controllers/reservations.js.
  - services/activityTracker.js.
  - routes/reservations.js already exposes PUT /api/reservations/pending-confirmation/:reservationId/:userId.
- Pending-confirmation decision behavior:
  - updatePendingConfirmationReservation supports action=confirm, action=reject, action=cancel, and revert-to-pending actions.
  - Reservation employees, managers, admins, and Hotel System Admin style actors may use the status decision workflow within their hotel scope.
  - Finance-only users remain restricted to finance/commission review and cannot make reservation-team confirm/reject/cancel decisions.
  - action=reject requires rejectionReason/rejectionComment/comment.
  - action=cancel now requires an explicit cancelReason/cancel_reason/rejectionReason/reason. It must not silently default to a generic cancellation reason.
- Reject semantics:
  - Reject sets reservation_status/state to Rejected.
  - pendingConfirmation.status becomes rejected.
  - pendingConfirmation.rejectionReason stores the reservation employee comment.
  - agentDecisionSnapshot.status becomes rejected with the same reason.
  - The agent sees the rejection notification and can update/resubmit the reservation for another hotel review.
- Cancel semantics:
  - Cancel sets reservation_status/state to cancelled.
  - cancel_reason stores the reservation employee cancellation reason.
  - pendingConfirmation.status and agentDecisionSnapshot.status become cancelled.
  - The agent sees a notification but cannot update the reservation because it is closed.
- Agent notification feed:
  - Agent pending-confirmation notifications now include pendingConfirmation.status/agentDecisionSnapshot.status = cancelled in addition to confirmed/rejected.
  - Cancelled reservation-team decisions are emitted as agent_decision-style notification rows with decisionStatus cancelled and the cancellation reason.
- Agent update permissions:
  - Order-taker/agent users can fully correct and resubmit their own reservations only when the reservation is still pending confirmation, rejected by the reservation team, or rejected by finance.
  - Full correction may include dates, rooms, pricing, comments, and guest details through the existing sanitizeOrderTakerUpdate path.
  - Once hotel/finance review has locked the reservation, agents can only update minor guest/customer details.
  - Minor guest/customer details include name, phone, email, passport, passportExpiry, nationality, copyNumber, car details, and confirmationNumber.
  - Agents cannot update checkin, checkout, rooms, pricing, total amount, status, payment/finance fields, or commission fields once the reservation is confirmed, in-house, checked-out, pending finance review, or otherwise locked.
  - Cancelled/no-show/closed reservations are not agent-editable at all.
- Activity tracker guard:
  - services/activityTracker.js must only recurse into actor.previewedBy when previewedBy exists.
  - This prevents normal reservation employee decisions from logging a harmless but noisy maximum call stack error during activity tracking.
- Workflow test performed after implementation:
  - Successful run key: codex-reservation-workflow-1780373168982.
  - Created Codex-marked owner, hotel, reservation employee, agent, and three reservations in the hotels database.
  - Confirm test: reservation employee action=confirm returned 200, pendingConfirmation.status became confirmed, and reservation_status moved to pending finance review.
  - Reject test: reservation employee action=reject returned 200, the rejection comment was stored in pendingConfirmation/agentDecisionSnapshot, and the agent notification feed returned decisionStatus rejected.
  - Agent correction test: the agent updated the rejected reservation's guest name and dates; updateReservation returned 200 and resubmitted the reservation to pending confirmation.
  - Cancel test: reservation employee action=cancel returned 200, cancel_reason was stored, pendingConfirmation/agentDecisionSnapshot became cancelled, and the agent notification feed returned decisionStatus cancelled.
  - Cancel lock test: the agent then attempted to update guest/dates on the cancelled reservation; updateReservation returned 403 with code agent_confirmed_reservation_locked and the reservation data remained unchanged.
  - Before production push, only this successful Codex workflow run was cleaned up: 3 reservations, 3 users, and 1 hotel were deleted by exact run key, with zero remaining matching reservations/users/hotels.
  - Backend checks passed after the workflow test:
    - node --check controllers/reservations.js.
    - node --check services/activityTracker.js.
  - Frontend npm.cmd run build also passed after the table/modal/notification UI updates.

2026-06-03 OTA Platform Review, Release, And Hotel Pricing Privacy
- Backend files:
  - services/otaReservationVisibility.js.
  - services/auditPrivacy.js.
  - controllers/otaInbound.js.
  - controllers/janat.js.
  - controllers/overall_dashboard.js.
  - controllers/reservations.js.
  - controllers/hotel_details.js.
  - controllers/adminreports.js.
  - services/otaReservationMapper.js.
  - services/notificationEvents.js.
- OTA email-created reservation lifecycle:
  - Successful OTA email reservation creation saves the reservation internally first, but non-cancelled OTA bookings must enter platform review before any hotel user can see or act on them.
  - Pending review reservations use reservation_status/state = "OTA Platform Review" and otaPlatformReview.status = pending.
  - The reservation remains connected to the inbound email audit record for duplicate detection, reconciliation, and traceability.
  - OTA duplicate emails are still saved as audit records, but duplicate_email processing must not create a second reservation or overwrite the original reservation.
- OTA inbound hotel-name matching:
  - services/otaReservationMapper.js resolves extracted OTA hotel names against PMS hotels after AI extraction; the AI should still preserve the email hotel text and avoid guessing PMS hotels.
  - Explicit exception: AlSukareya HOTEL / Al Sukareya / Al Sukareya Hotel is the same property as ZAD AL SAD / Zad Al Sad. The mapper expands only this exact alias group before scoring hotel matches.
  - Do not lower the global hotel-name match threshold or add broad fuzzy rules for this exception.
- Hotel visibility invariant:
  - Pending OTA platform-review reservations must not appear in any hotel-management, hotel owner, hotel system admin, reservation employee, order taker, hotel report, hotel details, overall dashboard, payment summary, search/list, or drill-down response.
  - Hotel-facing queries must use buildExcludePendingOtaReviewFilter, appendExcludePendingOtaReviewFilter, or the equivalent centralized guard from services/otaReservationVisibility.js.
  - Platform admin/SUPER admin OTA review screens may use buildPendingOtaReviewFilter to fetch only pending OTA reservations.
  - Do not add a new hotel-facing reservation endpoint without applying the pending OTA exclusion.
- Platform admin OTA review API:
  - GET /api/admin/ota-reservations/:userId returns pending OTA platform-review reservations for authorized platform users.
  - PUT /api/admin/ota-reservations/:reservationId/pricing/:userId updates the platform/admin pricing fields before hotel release.
  - PUT /api/admin/ota-reservations/:reservationId/release/:userId releases the reviewed reservation to the hotel workflow.
  - Access is limited to SUPER admins and internal platform employees with OTAReservations permission.
- Release behavior:
  - Release To Hotel changes reservation_status/state from "OTA Platform Review" to "Pending Confirmation".
  - Release To Hotel must fail when the OTA review has no positive Total base hotel price/adminPricing.rootTotal or when daily pricing rows do not have explicit base/root hotel prices.
  - The release endpoint returns ota_hotel_base_price_required / ota_daily_base_price_required-style errors for missing hotel-base pricing; this must remain server-side, not only a frontend disabled button.
  - Release sets otaPlatformReview.status = released and preserves audit metadata showing who released the reservation and when.
  - After release, the reservation should enter the normal reservation employee pending-confirmation workflow before final hotel confirmation.
  - Hotels must only receive normal hotel notifications after release; pending OTA review notifications are platform-only.
- OTA pricing privacy:
  - OTA/platform-managed reservations can carry both client/OTA gross pricing and hotel-visible/root pricing.
  - adminPricingVisibility.rootOnlyForHotelManagement = true means hotel-management users must see only the assigned root/hotel price.
  - Hotel-facing sanitization returns total_amount/sub_total as the hotel-visible root amount, removes adminPricing, removes otaPlatformReview, hides client/net/expense/margin fields, and keeps commission display from exposing the OTA gross amount.
  - Hotel-facing sanitization also removes OTA email audit keys from supplierData, including otaCreatedFromEmail, otaInboundEmailId, otaLastInboundEmailId, otaProvider, otaConfirmationNumber, and platformConfirmationNumber. Hotel-management users must not receive the classified inbound-email audit linkage after release.
  - Platform admins and SUPER admins may still see the raw OTA/client total and adminPricing breakdown.
  - Aggregations and scorecards in hotel-facing reports must use the same hotel-visible amount expression, not raw total_amount.
- Classified inbound email audit access:
  - GET /api/inbound-emails/single/:inboundEmailId and the inbound email list are restricted to active SUPER admins or platform users who canManageOtaReservations, which requires the OTAReservations admin permission.
  - Hotel owners, hotel system admins, reservation employees, reception, finance, and order takers must not be able to fetch inbound email audit bodies directly, even if a frontend route accidentally exposes an id.
- Notification behavior:
  - New pending OTA reservations should create a live platform-admin notification and bell sound opportunity in the admin UI.
  - Pending OTA review notifications must not be sent to hotel users.
  - Release can trigger the normal hotel-facing pending-confirmation notification/workflow.
- Important testing checks:
  - A pending OTA reservation should be present in /admin/ota-reservations and absent from /hotel-management/main-dashboard reservation lists and hotel reports.
  - After release, the reservation should become visible to hotel users as Pending Confirmation.
  - For flagged OTA/platform-managed reservations, hotel owner, hotel system admin, reservation employee, and order taker responses must show the assigned hotel/root amount, while platform admin responses can show the raw OTA/client amount.
- 2026-06-03 strict OTA QA completed:
  - Confirmations 1445133730, 4794240937, and 3331936952 were reverted to pending OTA Platform Review by exact confirmation-number filters.
  - Post-revert verification: buildPendingOtaReviewFilter saw all 3 and buildExcludePendingOtaReviewFilter saw 0, confirming admin-only visibility before release.
  - releaseOtaReservationToHotel now refuses release unless the OTA review has explicit positive adminPricing.rootTotal / Total base hotel price and explicit daily root/base pricing rows.
  - Hotel-facing sanitization must not fall back to client/OTA gross pricing when root/base values are missing.
  - canManageOtaReservations now treats OTA review as platform-admin/SUPER-admin work; hotel-side System Admin accounts cannot become OTA reviewers just by carrying an OTAReservations access string.
  - Local QA run codex-ota-strict-1780486464216 created only temporary Codex-marked records: 1 owner user, 1 hotel, and 2 OTA reservations.
  - QA assertions passed:
    - Platform admin with OTAReservations can manage OTA reservations.
    - Hotel-side System Admin with OTAReservations string cannot manage OTA reservations.
    - Release is blocked with a 422 when OTA pricing review/base hotel price is missing.
    - Pricing update with explicit client/root daily prices marks the reservation release-ready.
    - Release succeeds after explicit reviewed base hotel pricing.
    - Hotel viewer receives only the root/base amount and does not receive adminPricing or otaPlatformReview.
    - Hotel/non-platform viewer receives no OTA email audit supplierData keys and cannot fetch the inbound email audit endpoint.
    - Pending OTA reservation is admin-filter visible and hotel-management-filter hidden.
  - Cleanup deleted exactly the temporary 2 reservations, 1 hotel, and 1 user created by the QA run, then verified zero remaining matching temporary records.
  - Validation passed:
    - node --check controllers/janat.js.
    - node --check services/auditPrivacy.js.
    - node --check services/otaReservationVisibility.js.
    - hotels_frontend npm.cmd run build compiled successfully.

2026-06-03 Overall Financial Actions OTA Commission Display
- Backend file:
  - controllers/overall_dashboard.js.
- Route:
  - GET /api/overall-dashboard/financial-actions/:userId.
- Issue fixed:
  - Hotel-facing OTA pricing privacy correctly sanitizes released OTA/platform-managed reservations to the hotel-visible root/base total.
  - That sanitization zeros generic commission fields in the hotel-facing shape, while the valid assigned commission may remain in commissionData.amount.
  - The financial-actions API now adds a safe commissionAmount to each reservation row by resolving the raw reservation before sanitization and falling back to the sanitized row when needed.
- Privacy invariant:
  - Financial-actions may expose the assigned commission needed for finance review.
  - It must still not expose adminPricing, adminPricingVisibility, otaPlatformReview, OTA gross/client totals, platform margin, OTA expenses, or net-after-expense fields to hotel-management users.
  - Hotel-visible total_amount/sub_total must remain the assigned root/base hotel amount for adminPricingVisibility.rootOnlyForHotelManagement reservations.
- Production verification:
  - Reservation 3331936952 raw commission was 60 and commissionData.amount was 60.
  - The sanitized hotel-management row keeps total_amount/sub_total at 510 and hides admin pricing fields.
  - The financial-actions response shape now resolves commissionAmount 60, so the frontend modal displays 60 and 11.76% of 510.
  - node --check controllers/overall_dashboard.js passed before production restart.

2026-06-03 Expedia Partner Central Dry-Run Property Collector
- Backend script:
  - scripts/expedia/dryRunProperties.js.
- NPM shortcuts:
  - npm run expedia:properties:dry-run.
  - npm run expedia:properties:self-test.
- Purpose:
  - Small, read-only dry run for Expedia Partner Central browser access after the owner received approval to scrape/browser-automate within the approved Expedia support scope.
  - Opens Partner Central in a visible Puppeteer browser with a persistent profile, lets the human owner complete login and MFA manually, navigates to ManageProperty, and extracts the visible property list.
  - Saves a JSON audit and full-page screenshot under audits/expedia-dry-run by default.
- Safety boundaries:
  - This script does not read, store, or submit the Expedia password.
  - This script does not update room counts, rates, restrictions, inventory, reservations, or calendar data.
  - This script does not click property update controls or write to PMS/MongoDB.
  - MFA and any account-security challenge remain human-controlled.
  - Keep future Expedia automation scoped to the written Expedia approval/case wording, with dry-run and audit logs before any write-capable workflow.
- Useful environment variables:
  - EXPEDIA_DRY_RUN_PROFILE_DIR controls the persistent browser profile folder.
  - EXPEDIA_DRY_RUN_OUTPUT_DIR controls where JSON/screenshot audit files are saved.
  - EXPEDIA_DRY_RUN_TIMEOUT_MINUTES controls how long the script waits for manual login/MFA.
  - EXPEDIA_BROWSER_PATH can point to the installed Chrome/Edge binary on the home server.
  - EXPEDIA_DRY_RUN_KEEP_OPEN=true keeps the browser open after extraction for manual inspection.
  - EXPEDIA_BROWSER_NO_SANDBOX=true is available only for Linux/root server environments that require it.
- 2026-06-03 server dry-run verification:
  - Server path: /home/ahmedadmin/Hotels/hotels_backend.
  - Persistent Expedia browser profile path: /home/ahmedadmin/.jannatbooking/expedia-dry-run-profile.
  - Audit output path: /home/ahmedadmin/Hotels/hotels_backend/audits/expedia-dry-run.
  - Visible-browser runs from SSH require Windows VcXsrv plus ssh -Y jannat. The forwarded DISPLAY should look like localhost:10.0. Do not use DISPLAY=:0 unless a real server desktop exists.
  - Linux Chromium on jannat requires EXPEDIA_BROWSER_NO_SANDBOX=true.
  - Verified command:
    EXPEDIA_BROWSER_NO_SANDBOX=true EXPEDIA_DRY_RUN_TIMEOUT_MINUTES=20 EXPEDIA_DRY_RUN_KEEP_OPEN=true npm run expedia:properties:dry-run
  - Successful read-only extraction returned 7 Expedia properties:
    - Al-Magd Hotel, Property ID 120233712.
    - Al-Qemma Hotel, Property ID 120208625.
    - Al-Rehab Hotel, Property ID 120233940.
    - AlSukareya HOTEL, Property ID 120199112.
    - Taj Al Zahabiya Hotel, Property ID 119872210.
    - Zad Al Safa Hotel, Property ID 122352868.
    - Zad Al-Mashaer Hotel, Property ID 120211153.
  - Current parser limitation: the last property address can include trailing page text such as Add a property / Check for updates. Property name and Expedia property ID extraction are correct.
  - The Expedia session was reused on the second run through the persistent server browser profile.

2026-06-04 Admin All-Reservations OTA Pricing Summary And Edit Prefill
- Backend files:
  - controllers/janat.js adds ota_financial_summary to rows returned by GET /api/all-reservations-list-admin/:userId when an OTA/platform-managed pricing signal is present.
  - services/reservationPricing.js normalizes provided pricing rows so an explicit rootPrice remains the hotel/root value when the reservation is re-saved.
- Role and privacy rules:
  - Hotel-facing sanitization still removes adminPricing, adminPricingVisibility, otaPlatformReview, OTA audit supplierData keys, OTA expenses, net-after-expense fields, platform margin, and client gross pricing.
  - Admin all-reservations may expose a safe ota_financial_summary for internal platform employees so the frontend can show what the hotel sees and the total after OTA expenses.
  - platformProfit is included in that summary only for configured SUPER_ADMIN_ID / REACT_APP_SUPER_ADMIN_ID users; regular platform employees must not receive that safe-summary profit field.
- Summary fields:
  - clientTotal is the OTA/client gross reservation value.
  - clientPaidAmount is resolved from paid_amount_breakdown when present, otherwise paid_amount.
  - hotelVisibleAmount is adminPricing.rootTotal when present, otherwise the computed hotel-visible/root amount.
  - netAfterExpenses is adminPricing.netAfterExpensesTotal when present, otherwise clientTotal minus OTA expense, falling back to hotelVisibleAmount.
  - otaExpenseTotal is adminPricing.otaExpenseTotal when present, otherwise clientTotal minus netAfterExpenses.
- Edit/save behavior:
  - When the admin Edit Pricing modal posts saved daily rows, normalizeProvidedPricingDay now keeps totalPriceWithoutCommission aligned to explicit rootPrice for admin three-price/OTA rows.
  - This prevents previously saved OTA breakdowns such as reservation 1445133730 from reopening or saving with the client gross amount duplicated into the hotel/root no-commission field.

2026-06-04 Overall Rejected Reservations Workspace And Notifications
- Backend files:
  - controllers/overall_dashboard.js adds rejected-only reservation listing/export support.
  - routes/overall_dashboard.js exposes:
    - GET /api/overall-dashboard/rejected-reservations/:userId
    - GET /api/overall-dashboard/rejected-reservations-export/:userId
  - controllers/reservations.js keeps rejected reservations visible in the pending-confirmation notification feed and includes the rejection comment in notification payloads.
- Role and scope rules:
  - SUPER ADMIN, platform admin, owner-like/system admin, hotel manager, reservation employee, finance, and order-taking accounts use the same access family as Pending Reservations.
  - SUPER ADMIN/platform/admin users can see rejected reservations across their permitted hotel scope and can edit/resubmit them.
  - Pure order-taking/agent accounts remain scoped to their own reservations only.
  - Admin/SUPER ADMIN power does not bypass the cycle: rejected reservations are corrected through the details/edit flow and sent back to Pending Confirmation for hotel review.
- Rejected query behavior:
  - The rejected feed includes reservations whose rejection signal is in reservation_status, state, pendingConfirmation.status, agentDecisionSnapshot.status, financial_cycle.totalReviewStatus, or commissionAgentApproval.status.
  - Status filter value rejected now maps to that full rejected workflow filter instead of only matching the primary reservation_status text.
  - Scorecards and booking-source options use the same rejected-only scope so counts match the table.
  - Reservation list responses return compact hotel filter options only (_id, hotelName, belongsTo) instead of full hotel documents, keeping rejected/pending payloads small.
- Notification behavior:
  - Pending-confirmation notifications now include pendingConfirmation.status = rejected and agentDecisionSnapshot.status = rejected.
 - Notification rows include decisionStatus and decisionReason so platform/admin frontends can show the reservation employee rejection comment.
  - Finance rejections still route through finance review behavior, while hotel/reservation-team rejections can route to the rejected reservations workspace.

2026-06-04 Platform Admin Rejected Reservations Separation
- Backend files:
  - controllers/janat.js adds a platform-admin rejected reservation list/export implementation.
  - routes/janat.js exposes the platform-admin endpoints:
    - GET /api/admin/rejected-reservations/:userId
    - GET /api/admin/rejected-reservations-export/:userId
- Scope rule:
  - These /api/admin/* endpoints are intentionally separate from /api/overall-dashboard/*.
  - They do not call withPlatformReservationScope and therefore do not limit the list to hotelsToSupport / assigned hotels.
  - They are protected by requireSignin, isAuth, and requireAdminAccess("HotelsReservations", "AllReservations").
  - Configured SUPER ADMIN users and authorized platform reservation employees can load rejected reservations across all hotels from the platform-admin route.
- Rejected matching:
  - The query includes reservations rejected through reservation_status, state, pendingConfirmation.status, agentDecisionSnapshot.status, financial_cycle.totalReviewStatus, or commissionAgentApproval.status.
  - Pending OTA Platform Review reservations remain excluded by appendExcludePendingOtaReviewFilter.
  - Optional filters include reservationId, hotelId, bookingSource, check-in range, checkout range, created range, and text search.
- Response shape:
  - data rows include customer_name, customer_phone, customer_email, hotel_name, rejection_type, rejection_label, rejection_reason, rejected_at, and safe OTA financial summary where applicable.
  - ota_financial_summary still includes platformProfit only for configured SUPER ADMIN users through buildAdminOtaFinancialSummary.
  - scorecards include totalRejected, rejectedToday, hotelsWithRejections, clientTotal, hotelVisibleTotal, and rejected counts by hotel/finance/commission/reservation type.
  - hotels and bookingSources are compact filter-option arrays derived from the returned all-hotels rejected set.
- Workflow:
  - This endpoint only lists/export rejected reservations for the platform-admin workspace.
  - Corrections and resubmission still go through the existing reservation update flow, which pushes corrected rejected reservations back to Pending Confirmation.
